# Handoff — 2026-07-07 night (the program-execution session)

**`main` @ `e4163be`**, version **0.9.85.0** (changes under CHANGELOG `[Unreleased]`).
Supersedes [docs/handoffs/2026-07-07.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-07.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ Program plan (rows 3–5 EXECUTED this session; PR [#382](https://github.com/andrewzp/theheat/pull/382) still OPEN — Andrew's adoption gate):**
> `docs/superpowers/plans/2026-07-06-front-page-parity-plan.md` + per-row plans in
> `docs/superpowers/plans/front-page-parity/` (branch `docs/front-page-parity-plan`;
> INDEX statuses updated on that branch this session).

## TL;DR

Andrew said *"stop asking me to do things … just do the things that we need to do to
make this exceptional"* — the go signal. This session executed the program's build
rows in order. **Rows 3, 4, and 5 are MERGED** (five PRs), the **A1 pre-flip gate is
GREEN again** after finding and fixing a real three-gate contradiction, and the only
things left that a session cannot do are the classifier-owned clicks: **merge #382**
and **flip the three Bet A flags** (the auto-mode classifier correctly blocked both —
they are Andrew's).

## Merged this session (all codex-xhigh looped to clean APPROVE, all CI-green, squash-verified)

1. **[#384](https://github.com/andrewzp/theheat/pull/384) — row 3 PR-A:** the
   daily-plan grading corpus (A-rate history, failure-mode evidence) merged to main
   after 29 cycles marooned on `daily-plan-current`. Docs-only, +4862/−106.
2. **[#385](https://github.com/andrewzp/theheat/pull/385) — row 3 PR-B (v0.9.83.0):**
   `apply_forecast_elapsed_sweep` — elapsed-forecast pending drafts (the Basrah/Doha
   class, 38 grader-flagged cycles) auto-reject at the triage drain step. codex r1
   caught a REAL P1: GHCN also emits *observed* `absolute_extreme`, so the sweep is
   **provenance-aware** — sweeps only on the review_context `Data source: forecast`
   marker (rides every absolute_extreme draft since #195); GHCN/unknown provenance
   never sweeps (fail-safe; the 7d TTL still bounds). r2 clean APPROVE.
3. **[#386](https://github.com/andrewzp/theheat/pull/386) — row 4 (v0.9.84.0):**
   P_tier + P_dust voice floor. Writer WHAT-NEVER-SHIPS bullet "DETECTION PLUMBING IS
   NOT A FACT" (band_label / score thresholds / trigger definitions never citable;
   observed actuals + published scales stay) + fact-check rule (m) + critic
   `internal_taxonomy_leak` kill. Dust events carry a REAL anchor: co-measured PM10
   24h mean vs the WHO 2021 PM10 AQG (45 μg/m³), pre-computed
   (`who_pm10_multiple`), conditional bundle facts, Open-Meteo hourly 4→5 vars
   (watch air-quality coverage ≥90% for 2 cycles — contingency in the row-4 plan).
   `writer_dryrun --type dust` (Phalodi 20.0×). codex r1 clean APPROVE.
4. **[#387](https://github.com/andrewzp/theheat/pull/387) — the A1 gate contradiction
   fix:** the news-enrich-dryrun went RED on main (0/3 shipped, all critic
   `press_release_shape` kills). Root cause: E1's fire exemplar TEACHES the
   sourced-impact lead, rule (k) REQUIRES the source name in the text, and the critic
   killed exactly that shape — the A1 lane was structurally unshippable. Fix: one
   critic carve-out (sourced human-impact lead with mid/late-sentence attribution is
   evidence discipline, NOT press-release; the kill stays for agency/label OPENERS).
   Live re-run GREEN: ships + `decision4 → FORCED manual_only`. codex APPROVE.
5. **[#388](https://github.com/andrewzp/theheat/pull/388) — row 5 (v0.9.85.0), closes
   [#375](https://github.com/andrewzp/theheat/issues/375):** `cyclone_land_threat`.
   Forecast track parsed from stored JTWC
   warning text + a new NHC TCM fetch (`forecastAdvisory.url`); nearest-cities.csv
   landmass; one-shot (storm, landmass) pair state; forecast-tense-or-fail pairing
   (writer convention + fact-check rule n); threshold 70; `manual_only` by the
   `cyclone_` prefix. codex r1 BLOCKed with TWO REAL P1s — (a) the sqlite
   allowlist omitted the new pair key (dedup would silently die across runs);
   fixed + a NEW structural persistence contract test that pins EVERY
   DEFAULT_STATE key to a persistence path (it immediately caught
   `credential_expiry`/#330 as another latent instance, also fixed); (b) past
   NHC valid-times could mint a forecast-tense event about an already-passed
   approach; fixed with `now <= valid`. r2 clean APPROVE on the final head.

## Live state at handoff

- **Bet A flags: ALL THREE still unset** (classifier blocked the flip — correctly;
  they are Andrew's). The A1 enrich pre-flip gate is GREEN on current main
  ([run 28843128016](https://github.com/andrewzp/theheat/actions/runs/28843128016)).
- **#382 still OPEN, zero comments** — the program-adoption gate (classifier blocked
  my merge; Andrew's click). Its branch got INDEX status marks for rows 3/4/5.
- **Canary Monday GREEN** (time-travel-canary success 07-06 12:14Z). All bot +
  sentinel runs green through the session.
- **Bavi (Typhoon 09W): still an active warned storm** — JTWC Warning #25 (07-07
  0300Z) DOWNGRADED it from super typhoon (125 kt, re-strengthening forecast, WNW
  toward the Taiwan region). The old hand-draft text is DEAD ("still a super
  typhoon" is now false — do not post). **Bavi is row 5's live verify case**: after
  the next bot cycles, a qualifying JTWC forecast should mint ONE
  `cyclone_land_threat` manual draft for its first landmass approach. Watch the
  dashboard.
- **Barrow B+ draft still pending** (Jul 4 carry-over) — Andrew's dashboard reject,
  plus the posted 2026-06-07 Barrow tweet delete decision.
- **#308 yield watch:** `nhc` root-caused live this session — `CurrentStorms.json`
  returns `activeStorms: []`, genuinely quiet Atlantic (comment on the issue).
  `reanalysis_anomaly` + `usgs_quakes` still advisory-watch.
- **#309 SELFHEAL_PAT** (Andrew), **#346 + #324 held**, **#372 remainder** (Andrew's
  coverage calls) — unchanged.
- Daily-plan routine alive (#207 fresh 07-06; next cycles will grade against the
  now-on-main corpus).

## ANDREW'S TWO CLICKS (everything else is done or automatic)

1. **Merge [#382](https://github.com/andrewzp/theheat/pull/382)** — adopts the
   program formally (rows 3–5 are already merged as code; the plan docs + remaining
   rows 6–14 execute from the INDEX after this lands).
2. **Flip the Bet A master** (then enrich → boost per the track-0 runbook, each
   verified 2–3 cycles, one-flip rollback):
   ```bash
   gh variable set THEHEAT_NEWSWORTHINESS_ENABLED --body 1 --repo andrewzp/theheat
   # after A0 verifies (news_events citations + news-gap sanity):
   gh variable set THEHEAT_NEWS_ENRICH_ENABLED --body 1 --repo andrewzp/theheat
   # after A1 verifies (impact drafts manual-only):
   gh variable set THEHEAT_NEWS_BOOST_ENABLED --body 1 --repo andrewzp/theheat
   ```
   The A1 pre-flip gate is already GREEN on current main; any session can run the
   verify steps (they are read-only).

Standing decisions unchanged: `THEHEAT_AUTOSHIP_MAX_AGE_H` (36→96), #309 PAT, #324,
#346, the posted Barrow tweet delete.

## NEXT SESSION (after Andrew's clicks, in order)

1. **Live-verify row 5 on Bavi** (it should have minted a manual draft — check the
   dashboard + `cyclone_land_threat_pairs` in state; the plan's Task 10 live-verify
   steps). Dispatch `writer-dryrun --type cyclone_land_threat` and
   `--type dust` once and read the drafts (voice check; needs API keys = the
   workflow, not local).
2. **Track-0 verify duties** for whichever flags Andrew flipped (runbook checklists).
3. **Row 6 (editor brief)** — corpus is on main now (its "row 3 recommended first"
   precondition is satisfied); then row 7 (precip four-moves, row-4 pattern), row 14
   (two small PRs), row 9 (needs Andrew's X-API-tier confirmation), row 8 (FPP —
   live data starts at the master flip).
4. **Watch air-quality coverage** ≥90% for 2 cycles post-#386 (the 4→5 hourly-var
   note); contingency = dust-cities-only pm10 request (row-4 plan Task 1 note).
5. Re-merge `daily-plan-current` docs when the routine's next cycles land (one
   command now: `git merge origin/daily-plan-current` — verified conflict-free
   pattern).

## Standing rules (unchanged)

The full battery: INDEX §Standing rules. Session lessons that held: every codex
BLOCK finding was real (the GHCN provenance P1 — a false-rejection class no
same-model pass caught); the `cd … && PATH=…` prefix on EVERY command; worktrees
(`theheat-rows`, since removed: `theheat-a1fix`) keep the primary tree parked on
`docs/front-page-parity-plan` so Andrew's #382 links resolve.
