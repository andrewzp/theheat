# Handoff — 2026-07-07 noon (program adopted, Bet A master LIVE)

**`main` @ `58bdaa4`**, version **0.9.85.0** (changes under CHANGELOG `[Unreleased]`).
Supersedes [docs/handoffs/2026-07-07-night.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-07-night.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ Program (ADOPTED — #382 merged 2026-07-07):**
> [docs/superpowers/plans/2026-07-06-front-page-parity-plan.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-07-06-front-page-parity-plan.md)
> + [docs/superpowers/plans/front-page-parity/INDEX.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/front-page-parity/INDEX.md)
> — all on main now. Rows 3/4/5 MERGED; INDEX statuses current.

## TL;DR

Andrew gave explicit permission and the two held gates opened: **#382 is merged**
(the front-page-parity program is formally adopted, plan docs on main) and
**`THEHEAT_NEWSWORTHINESS_ENABLED=1` is LIVE** (flipped 2026-07-07 12:59:59Z).
A0's first-light cycle (manually dispatched run
[28868097367](https://github.com/andrewzp/theheat/actions/runs/28868097367)) ran
GREEN: the flag reached the run, the retrieval lane executed with no errors, and —
bonus — **row 4's `internal_taxonomy_leak` critic kill fired twice on real drafts
in its first live cycle** (P_tier dying in production). Row 5 stayed correctly
quiet on Bavi (closest 72h forecast point ≈220 NM from Taipei — outside the 150 NM
trigger; it should fire as the track closes).

Everything merged this session (rows 3/4/5 + the A1-gate fix) is recorded in
[docs/handoffs/2026-07-07-night.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-07-night.md)
— that doc remains the detailed build record; THIS doc is the live-state pointer.

## Flag state (the rollout ladder)

| Flag | State | Next gate |
|---|---|---|
| `THEHEAT_NEWSWORTHINESS_ENABLED` (A0 master) | **1 — LIVE since 2026-07-07 12:59:59Z** | verify 2–3 cycles (runbook Step 1 checklist) |
| `THEHEAT_NEWS_ENRICH_ENABLED` (A1) | unset (0) | flip after A0 verifies; **pre-flip gate already GREEN** ([run 28843128016](https://github.com/andrewzp/theheat/actions/runs/28843128016), post-#387); **prod flips need Andrew's explicit go** |
| `THEHEAT_NEWS_BOOST_ENABLED` (A2) | unset (0) | flip after A1 verifies 2–3 cycles |

Rollback for any step: `gh variable set <FLAG> --body 0 --repo andrewzp/theheat`.
Runbook: [docs/superpowers/plans/front-page-parity/track-0-runbook.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/front-page-parity/track-0-runbook.md).

**A0 verification scorecard (Step 1 checklist):**
- [x] Flag reaches the run + lane executes, no errors (first-light run 28868097367)
- [x] No runaway LLM calls (single retrieval sequence; ≤5 by design)
- [ ] `state["news_events"]` citations hand-checked (2–3 URLs) — needs dashboard or
      the sentinel's news-gap output; do on the 16:00Z + 20:00Z cycles
- [ ] News-gap watch sanity (sentinel runs every ~4h; a gap issue may appear —
      each listed gap must be a real world event we genuinely lack)
- [x] Zero editorial surface confirmed (cycle drafted normally: 1 draft saved)

## Live state

- **Merged this train (all codex-looped clean, squash-verified):** #384, #385
  (v0.9.83.0), #386 (v0.9.84.0), #387 (A1-gate critic fix), #388 (v0.9.85.0,
  closes #375), #389 (night handoff), #382 (the program). Three-pillar rows 1–8 +
  successor rows 3/4/5 ALL MERGED.
- **Bavi (Typhoon 09W): active warned storm**, ~125 kt, WNW toward the Taiwan
  region. Row 5's live verify case: expect the first `cyclone_land_threat`
  `manual_only` draft within a cycle of a qualifying forecast (≤150 NM of a
  curated city, ≤72 h). The OLD hand-draft text (2026-07-06-evening §Bavi) is
  DEAD — JTWC downgraded Bavi from super typhoon in Warning #25; never post it.
- **Open issues:** [#390](https://github.com/andrewzp/theheat/issues/390) state
  gist 1.38MB trend watch (NEW — advisory; truncation cliff itself is class-fixed);
  #308 yield watch (`nhc` root-caused: genuinely quiet Atlantic — verified live;
  `reanalysis_anomaly` + `usgs_quakes` still advisory); #309 SELFHEAL_PAT
  (Andrew); #372 remainder (Andrew's coverage calls).
- **Open PRs:** #346 (dup-city, held), #324 (claim/warrant, held), #207
  (daily-plan rolling).
- **Andrew's dashboard items:** reject the pending Barrow B+ precip draft; decide
  the POSTED 2026-06-07 Barrow tweet (recommendation: delete);
  `THEHEAT_AUTOSHIP_MAX_AGE_H` 36→96 decision.
- **Watch:** air-quality source coverage ≥90% for 2 cycles post-#386 (hourly vars
  4→5; contingency = dust-cities-only pm10 request, row-4 plan Task 1 note);
  first graded corpus cycle post-row-4 should show P_tier → 0 and dust drafts
  carrying the WHO anchor.

## Next work, in order (rows from the INDEX — now on main)

1. **Track-0 watch duties** (read-only, any session): A0 citations + news-gap
   sanity on the next 2 cycles → surface the evidence and get Andrew's go for the
   **enrich flip** → after A1 verifies 2–3 cycles (impact drafts manual-only,
   figures+sources verbatim), the **boost flip** → then watch the suppression
   ledger for `news_boost=` provenance.
2. **Row 5 live verify on Bavi** — first land-threat draft appears exactly once
   per (storm, landmass); verify forecast tense + `manual_only` on the dashboard;
   optionally dispatch `writer-dryrun --type cyclone_land_threat` and
   `--type dust` once and read the drafts (voice check).
3. **Row 6 — editor brief** ([row-06-editor-brief.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/front-page-parity/row-06-editor-brief.md));
   its row-3 precondition is satisfied (corpus on main).
4. **Row 7 — precip four-moves** (row-4 pattern), then **row 14** (two small
   PRs). **Row 9** needs Andrew's X-API-tier confirmation first; **row 8** (FPP
   rollup) gains real data now that the master is live — build after a few days
   of `news_events` accumulate; **rows 10/12/13** stay gated per the INDEX.
5. **Corpus re-merge cadence:** `git merge origin/daily-plan-current` is now a
   one-command routine — re-merge after a few daily cycles so the grading
   instrument stays current on main.

## Standing rules (unchanged — INDEX §Standing rules binds every task)

`cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH`
on EVERY command (incl. codex + git). Before every push: ruff + mypy + 90-day
canary pytest all green. codex-xhigh looped to clean APPROVE on editorial/state/
posting/storage diffs, LAST round after LAST edit. Merge = checks green → squash
→ `git log origin/main --oneline -1` shows your PR. Honesty gates only tighten.
Session lessons that held: EVERY codex BLOCK finding was real again (GHCN
provenance, sqlite allowlist, past valid-times); prod flag flips + program
adoption are classifier-owned — get Andrew's explicit words.

---

## Paste-ready kickoff prompt for a fresh session

> Pick up @theheat. `cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH`
> — that exact prefix on EVERY Bash command INCLUDING codex and git invocations
> (fresh shells lose cwd; codex then fails "Not inside a trusted directory", git
> exits 128 — treat those as the cwd slip, retry with the prefix). Python
> `.venv/bin/python`; repo `andrewzp/theheat`. `main` is `58bdaa4`+, version
> 0.9.85.0, tree on `main` (no parked branch — #382 is MERGED).
>
> **READ FIRST, in order:** (1) `docs/handoffs/2026-07-07-noon.md` — live state,
> flag ladder, A0 scorecard. (2) `docs/superpowers/plans/front-page-parity/INDEX.md`
> — row statuses + §Standing rules (they bind every task). (3) The track-0 runbook
> `docs/superpowers/plans/front-page-parity/track-0-runbook.md` for verify steps.
>
> **CONTEXT:** The front-page-parity program is ADOPTED (#382 merged) and rows
> 3/4/5 are MERGED (#384/#385/#386/#388 + the #387 A1-gate critic fix). **Bet A's
> master flag `THEHEAT_NEWSWORTHINESS_ENABLED=1` went LIVE 2026-07-07 12:59:59Z
> with Andrew's explicit permission** — A0 first-light cycle (run 28868097367) was
> GREEN (flag reached run, lane executed, no runaway; row 4's
> internal_taxonomy_leak kill already fired twice on real drafts). Enrich + boost
> flags still unset; the A1 pre-flip gate is GREEN (run 28843128016). Typhoon
> Bavi (09W) is an active warned storm — row 5's live verify case (correctly
> quiet so far: closest forecast point ≈220 NM from Taipei > the 150 NM trigger).
> The Sonnet 5 writer swap stays PARKED (2026-07-06-evening §Parked — do NOT
> re-investigate). #372's merged halves — do NOT re-fix. Rows 10/12/13 stay
> gated per the INDEX.
>
> **YOUR WORK, in order:**
> 1. **Verify live state:** fresh `git log origin/main -5`, `gh pr list`,
>    `gh issue list`, `gh variable list --repo andrewzp/theheat` (expect
>    NEWSWORTHINESS=1, no ENRICH/BOOST). Then **A0 verification** on the cycles
>    since 13:00Z per the runbook Step 1 checklist: newsworthiness source row
>    success/degraded (never `skipped`); hand-check 2–3 `news_events` citation
>    URLs (dashboard, or the sentinel's news-gap issue); news-gap sanity; ≤5
>    LLM calls/cycle. ANY missing/mismatched warrant → roll back the master
>    (`--body 0`) and investigate. One-flip rollback discipline throughout.
> 2. **Row 5 live verify:** check whether Bavi minted a `cyclone_land_threat`
>    draft (dashboard pending queue + `cyclone_land_threat_pairs` in state).
>    If yes: verify forecast tense, `manual_only`, exactly one per landmass —
>    then comment the evidence on #375's closing PR #388. If the storm's track
>    now qualifies but no draft appeared, debug per row-05 plan Task 10.
> 3. **When A0's scorecard is fully checked:** surface the evidence to Andrew
>    and request his explicit go for the **enrich flip** (prod flips are
>    classifier-owned; his words required). After his go:
>    `gh variable set THEHEAT_NEWS_ENRICH_ENABLED --body 1 --repo andrewzp/theheat`,
>    then watch 2–3 cycles: impact-citing drafts land `manual_only` ONLY
>    (autoship-armed impact draft = P0, roll back), figures verbatim + sources
>    named. Boost follows the same pattern after A1 verifies.
> 4. **Execute the next program rows from the INDEX in order:** row 6 (editor
>    brief; precondition satisfied), row 7 (precip four-moves), row 14 (two
>    small PRs). Each plan is self-contained: task-by-task
>    (superpowers:subagent-driven-development or superpowers:executing-plans),
>    TDD exactly as written, one PR per plan, codex-xhigh looped to clean
>    APPROVE where marked mandatory, LAST round after LAST edit. Row 9 first
>    needs Andrew's X-API-tier confirmation; row 8 after a few days of
>    news_events data.
>
> **WATCH ITEMS:** air-quality coverage ≥90% (2 cycles post-#386; contingency in
> row-4 plan); #390 state-size trend (advisory — log the size line per cycle);
> Barrow B+ draft reject + posted 2026-06-07 Barrow delete (Andrew, dashboard);
> `THEHEAT_AUTOSHIP_MAX_AGE_H` 36→96 (Andrew); #308 (`reanalysis_anomaly` +
> `usgs_quakes` only — `nhc` is root-caused); #309 SELFHEAL_PAT (Andrew); #346 +
> #324 held; corpus re-merge from `daily-plan-current` after a few cycles; first
> post-row-4 graded cycle should show P_tier → 0.
>
> **STANDING RULES (non-negotiable):** CI `test` = ruff + mypy + pytest +
> dashboard build. Before EVERY push: `ruff check src/ tests/` AND `mypy src/`
> AND `THEHEAT_TIME_TRAVEL_DAYS=90 .venv/bin/python -m pytest -q` all green;
> fixture dates today-relative, never literals. codex-xhigh =
> `cd /Users/andrewpuschel/Documents/Claude/theheat && codex exec -c model_reasoning_effort='"xhigh"' "<prompt>" < /dev/null`
> run in background, ONE backgrounding layer, looped to clean APPROVE (zero
> P0/P1/P2), LAST round STARTS after the LAST code edit — mandatory on anything
> touching editorial gates / posting / state / storage. Merge chains verify END
> STATE: checks green → squash-merge → `git fetch origin && git log
> origin/main --oneline -1` shows your PR (never trust `--watch` exit alone;
> checks can lag registration — wait and re-watch, never `--admin`). Claude
> merges PRs (Andrew never does). Stage only files your plan names; VERSION +
> CHANGELOG ride code PRs; docs/handoffs as their own PR. Dispatch-workflow
> inputs via `INPUT_*` env only. Never weaken honesty gates; every impact
> figure needs source+url+as_of. Keep Python↔JS mirrors in sync. New
> DEFAULT_STATE keys MUST ride the sqlite persistence contract test. Work
> autonomously; stop only for a real fork or a prod flag flip (classifier-owned
> — Andrew's explicit words required).
