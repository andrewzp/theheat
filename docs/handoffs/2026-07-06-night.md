# Handoff — 2026-07-06 (night)

**`main` @ `3279ff5`**, version **0.9.82.0** (changes under CHANGELOG `[Unreleased]`).
Supersedes [docs/handoffs/2026-07-06-evening.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-06-evening.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ Program plan:** [docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md)
> — queue rows 1–8 BUILT AND MERGED (row 8 = E1, this session). Remaining: R4
> (SELFHEAL_PAT, Andrew), A3 (v2, gated on gap-flag evidence).
> **★ Bet A spec:** [docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md).

## TL;DR

**E1 shipped ([#379](https://github.com/andrewzp/theheat/pull/379)) — the fire voice
floor.** The reganom four-moves pattern generalized to `fire` + `fire_footprint`,
every loosening paired with a fact-check rule in the same PR, the A1 impact citation
wired in as the fourth move, and a per-type dryrun harness
(`scripts/writer_dryrun.py --type fire|fire_footprint` + `writer-dryrun.yml`).
codex-xhigh: round 1 BLOCK (4 real P1s — workflow input injection, exemplar↔fixture
attribution drift, the old ticker exemplar, an area-digits bug), round 2 clean
APPROVE after fixes + regression locks. Follow-up **[#380](https://github.com/andrewzp/theheat/pull/380)**
closed the same injection class in `reganom-dryrun.yml` + `news-enrich-dryrun.yml`.

**Bet A is still fully DARK — verified live this session.** None of the three flags
exist in `gh variable list`; there was nothing to live-verify. All three flips remain
Andrew's, in spec order: **master → enrich → boost**.

## What shipped (all merged)

- **E1 ([#379](https://github.com/andrewzp/theheat/pull/379), v0.9.82.0):**
  - Writer prompt: a dedicated fire-bundles section naming the four moves —
    (1) lead with the event, not the detection; (2) scale words over raw units
    (`frp_tier` anchor; footprints cite new pre-rounded `area_km2_approx` /
    `area_acres_approx` with "about", never self-converted); (3) a fire has a
    name ONLY when the bundle names it (`complex_name` or a `human_impact`
    claim — hotspots are nameless); (4) stakes the bundle can see, each impact
    figure naming its OWN entry's source. Before→after exemplar
    (Colorado-class, tier-consistent, fixture-consistent); APPROVED EXEMPLARS
    #4 rewritten off the retired ticker opener.
  - Fact-check rule (l), the pairing: fire names/areas/operational
    specifics/behavior are bundle-or-nothing; tier words verify against
    `frp_tier` exactly; the "about" area forms are BUNDLE_FACT, not hedging.
  - Intern: `build_fire_footprint_bundle` pre-computes `area_km2_approx`
    (ha/100) + `area_acres_approx` (×2.47105, 3 sig figs) — the
    `value_rounded_c` pattern.
  - Harness: `writer_dryrun.py` runs the full chain (evidence contract →
    writer → safety → §F → fact-check → critic → decision-4 print),
    impact-carrying by default, `--no-impact` control, exit 2 on missing keys.
    **Works with all Bet A flags OFF** (it attaches fixture impact directly,
    like news-enrich-dryrun) — fire voice iteration needs no flips.
  - Regression locks: exemplar digits ↔ intern rounding; exemplar sources ↔
    harness DEFAULTS; ticker-opener ban; tier-word consistency.
- **Injection class-fix ([#380](https://github.com/andrewzp/theheat/pull/380)):**
  dispatch inputs now reach all three dryrun workflow shells via `INPUT_*`
  env vars only (data, not code) — `${{ inputs.* }}` in a `run:` block whose
  env holds API keys executes attacker-shaped input as shell. No semantic
  change; news-enrich stays fail-loud, and **Andrew's required pre-flip
  re-dispatch will live-verify the hardened workflow**.

## Bet A rollout (unchanged, waiting on Andrew — spec order)

1. **Master:** `gh variable set THEHEAT_NEWSWORTHINESS_ENABLED --body 1 --repo
   andrewzp/theheat` (A0: zero editorial surface, ≤5 Gemini calls/cycle). Watch 2–3
   cycles: `newsworthiness` source-health row, `state["news_events"]` citations
   (hand-check 2–3 URLs), news-gap issue sanity.
2. **Enrich:** re-run `news-enrich-dryrun` GREEN (now also covers #380's hardening) →
   flip `THEHEAT_NEWS_ENRICH_ENABLED` → impact-carrying drafts land ONLY in manual
   review.
3. **Boost:** flip `THEHEAT_NEWS_BOOST_ENABLED` → watch the suppression ledger for
   `news_boost=` reasons on rescued/killed fires.

One-flip rollback per stage. After enrich is live, `writer-dryrun.yml --type fire`
is the fire-voice iteration loop on real prompt behavior.

## Watch items

- **Pending Barrow precip draft** — still assumed pending (dashboard-auth'd; not
  verifiable from CLI). Reject it on the dashboard; #376 stops regeneration. If it
  sits, the queue watch (#364) goes loud at the 24h mark — the system failing
  loudly as designed. Also still open: the posted 2026-06-07 Barrow tweet
  delete/correct decision.
- **Issue [#308](https://github.com/andrewzp/theheat/issues/308)** (yield watch):
  `nhc`, `reanalysis_anomaly`, `usgs_quakes` green with zero observations across
  their windows. Plausibly seasonal/quiet (Atlantic lull; reganom recency window);
  advisory (`unknown` label), auto-closes when yield returns. Worth a look only if
  it persists into week 2.
- Canary Monday (time-travel suite); writer/queue watches quiet — no open sentinel
  issues besides #308/#309.

## ⭐ NEXT SESSION

1. **Rollout watching** per the Bet A section above, for each flag Andrew has
   flipped (live verifies + one-flip rollback if anything smells).
2. **[#375](https://github.com/andrewzp/theheat/issues/375) — `cyclone_land_threat`
   event class** (now the top build item): a warned storm whose JTWC/NHC FORECAST
   track brings its center or 34kt wind radius within N NM of a named landmass
   within M hours → ONE `manual_only` draftable event per storm-landmass pair
   (dedup key), forecast-tense language only. New state + editorial surface →
   full plan-first treatment + codex-xhigh loop. Bavi's Taiwan/China approach was
   the motivating live case; check whether it is still in a warned/approach phase
   when the session starts.
3. **#372 remainder** (Andrew's coverage calls, NOT re-fixing the merged halves):
   daily-record-path stuck guard, cross-source witness plausibility, ≤60° band.
4. Held: #346 dup-city (alias dedup direction), #324 claim/warrant review.

## ACTIONS FOR ANDREW

1. **Reject the pending Barrow precip draft** on the dashboard; decide on the
   POSTED 2026-06-07 Barrow tweet (recommendation: delete).
2. **Post the Bavi hand-draft** (in the evening handoff §Bavi) if it reads right —
   time-sensitive; Bavi's window is closing.
3. **Flip the master** (`THEHEAT_NEWSWORTHINESS_ENABLED=1`) when ready; enrich/boost
   follow in spec order after live verifies.
4. **Autoship ceiling** decision for lagged sources (`THEHEAT_AUTOSHIP_MAX_AGE_H`,
   default 36; GHCN records need ~96 to ever auto-ship).
5. Standing: `SELFHEAL_PAT` (#309); #324 review; #346 direction.

## Standing rules (unchanged)

- `cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH`
  on EVERY Bash command; Python `.venv/bin/python`; repo andrewzp/theheat.
- CI `test` = ruff + mypy + pytest + dashboard build; run ruff + mypy AND the local
  canary (`THEHEAT_TIME_TRAVEL_DAYS=90 … pytest -q`) before pushing; fixture dates
  today-relative.
- codex-xhigh (`< /dev/null`, background, ONE backgrounding layer, looped to clean
  APPROVE, LAST round after LAST code edit) on gate/posting/state/storage diffs.
- Merge chains verify end-state (squash commit on origin/main), never
  `gh pr checks --watch` exit alone. Andrew never merges.
- Never weaken honesty gates; every impact figure needs source+url+as_of.
- Dispatch-workflow inputs reach shells via env only (#380) — keep the pattern in
  any new workflow_dispatch.

---

## Paste-ready kickoff prompt for a fresh session

> Pick up @theheat (`cd /Users/andrewpuschel/Documents/Claude/theheat`;
> `PATH=/opt/homebrew/bin:$PATH` + that `cd` on EVERY Bash command; Python
> `.venv/bin/python`; repo andrewzp/theheat). `main` is `3279ff5`+, version 0.9.82.0.
> **Read `docs/handoffs/2026-07-06-night.md` FIRST**, then the program plan and Bet A
> spec it links.
>
> **Context:** three-pillar rows 1–8 are MERGED (row 8 = E1 fire voice floor, #379 +
> #380). Bet A is fully built and DARK behind `THEHEAT_NEWSWORTHINESS_ENABLED`
> (master) → `THEHEAT_NEWS_ENRICH_ENABLED` → `THEHEAT_NEWS_BOOST_ENABLED`, all
> unset=0. #372's gpm class is FIXED (#376) — only its remainder is open (Andrew's
> call). The Sonnet 5 writer swap is PARKED (evening handoff §Parked) — do not
> re-investigate unless Andrew asks.
>
> **Your main work, in order:**
> 1. Verify live state (fresh `git log origin/main`, open PRs/issues, `gh variable
>    list`). For each Bet A flag Andrew HAS flipped, run its live verify (handoff
>    §Bet A rollout: A0 = news_events citations + news-gap sanity; A1 = dryrun
>    re-run green + forced-manual drafts; A2 = news_boost reasons in the ledger).
>    One-flip rollback if anything smells. Surface any flag still pending.
> 2. **#375 — `cyclone_land_threat` event class** (handoff §NEXT SESSION item 2):
>    plan-first (brainstorm → written plan → eng review), then TDD; codex-xhigh
>    mandatory (new state + editorial surface). Check Bavi's current phase first.
> 3. **E1 live iteration** if flags are up: dispatch `writer-dryrun.yml`
>    (`--type fire`, then `fire_footprint`) and iterate the fire section against
>    real gate verdicts.
>
> **Standing rules:** CI test = ruff + mypy + pytest + dashboard build; local canary
> (`THEHEAT_TIME_TRAVEL_DAYS=90 .venv/bin/python -m pytest -q`) before pushing;
> fixture dates today-relative. codex-xhigh (`< /dev/null`, background, ONE
> backgrounding layer, looped to clean APPROVE, LAST round after LAST edit) on
> anything touching editorial gates / posting / state / storage. Keep Python↔JS
> mirrors + allowlists in sync. Stage only your files; docs as their own PR. Andrew
> never merges — verify the squash landed on origin/main, then squash-merge on
> green. Dispatch-workflow inputs via env only. Never weaken the honesty gates;
> every impact figure needs source+url+as_of. Work autonomously; stop only for a
> real fork or a prod flag flip (the classifier owns those).
