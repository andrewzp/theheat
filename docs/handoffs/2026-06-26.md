# Handoff — 2026-06-26

**`main` @ `6fbd3a7`.** Version line still **0.9.81.0** (both changes land under CHANGELOG
`[Unreleased]`, per the accumulate-until-release precedent).

## TL;DR

Verification of last session's `provider=both` fix found the **Open-Meteo world half was
~98% rate-limited** — the European-heatwave fix was observing ~2% of the world
(`world[readings:12 failures:584]`). Root cause: the world half did a **live 30-year
archive pull per city, every run**, and Open-Meteo's free archive endpoint 429s after
~12 cities (reproduced locally: forecast 22/22 OK, archive 14×`200` then
`429 "Minutely API request limit exceeded"`). The failures were a silent internal
counter, so monitoring stayed green. Fixed in two moves:

1. **Interim cap** ([#337](https://github.com/andrewzp/theheat/pull/337), merged) — budget-sized, urgent-first
   slice so the European cities are observed instead of starved. **Confirmed live**: the
   06-26 07:54 UTC cron dropped from 346s → **28s**, no 429 wall.
2. **The proper fix** ([#338](https://github.com/andrewzp/theheat/pull/338), merged) — a **world threshold
   cache** mirroring the GHCN/US half. Removes the cap; restores full ~595-city coverage
   at $0. 2× codex-reviewed (spec + plan), 11-task TDD build, Opus whole-branch review
   = READY TO MERGE.

## What shipped

### Interim cap — #337 (`c0732da`)
`select_world_budget_cities` (`src/data/open_meteo.py`) caps the `both` world half to
`WORLD_FETCH_BUDGET=10`, urgent-first (`URGENT_WORLD_HEAT_CITIES`: Madrid, Sevilla,
Zaragoza, Lyon, Paris + Jacobabad, Kuwait City, Mecca, Delhi, Ahvaz). Stopped the
bleeding while the proper fix was built. **Superseded by #338** (the cap is removed; the
`URGENT_WORLD_HEAT_CITIES` list now drives warm ordering).

### World threshold cache — #338 (`6fbd3a7`)
The architectural root cause was an asymmetry: the GHCN/US half is backed by a
precomputed threshold cache (cheap per-run compare); the world half recomputed 30 years
of history live every run. #338 gives the world half the same model:
- **Warm path** (`_run_world_cached_half`, `src/orchestrator/sources/open_meteo.py`):
  each run refreshes the ~8 stalest/missing cities' 30-yr archives (urgent-first), paced
  under an Open-Meteo weight budget; thresholds stored in a **separate gist file**
  (`world_threshold_cache.json`) — `state.json` untouched.
- **Hot path**: cheap multi-location forecast for cached cities vs cached thresholds —
  no archive call.
- **Budget + 429-resilience** (`src/data/openmeteo_budget.py`): a process-level
  `OpenMeteoBudget` paces the run under 600/min (with leaderboard headroom reserve); an
  Open-Meteo 429 now raises `OpenMeteoSaturated` and stops the run with **degraded**
  status — the original *silent* saturation is structurally impossible.
- **Field/month-wise cache merge** (`src/orchestrator/world_cache.py`): newest `as_of`
  wins per city; equal `as_of` keeps the more-extreme value per field — concurrent
  same-day writes never erase each other.
- **Anti-re-fire**: a fired record writes a provisional threshold so the same
  "hottest ever" can't re-tweet daily for 30 days.
- **Country-record coverage floor**: a national record only fires when enough of that
  country's cities are cached (no false "Spain record" from 1 city during bootstrap).
- **Failure classifier** (`classify_world_status`): steady-state degrades on low
  coverage / forecast-failure spike / saturation; bootstrap degrades only if the cache
  **stalls** run-over-run.
- **Scope**: `calendar_date` / `record_streak` / `simultaneous_records` are now
  **US-only** for the world (gist-prohibitive to cache; weakest tiers).
- Modules: `src/data/world_thresholds.py` (`CityThresholds`, `compute_city_thresholds`,
  `evaluate_city`), `src/orchestrator/world_cache.py`, `src/data/openmeteo_budget.py`.
- Spec: `docs/superpowers/specs/2026-06-26-world-threshold-cache-design.md`; plan:
  `docs/superpowers/plans/2026-06-26-world-threshold-cache.md` (both 2× codex-reviewed).

## Rollout watch (operational — the next session should check this)

The cache starts cold. Over **~10 days** it warms (8 cities/run × ~6 runs/day,
urgent-first), then self-refreshes on a 30-day TTL. Watch the gist state
(`gh gist view 06c02c97ffc0d11458687f1ed998d9e5 -f state.json`), the
`open_meteo_extreme_signals` source `details.open_meteo_pipeline_metrics`:
- `cached_count` should climb toward `world_total` (~595); `coverage_ratio` rises; no
  `saturated:true`; the `both` note shows `world[cached:X/595 cov:Z ... sat:False]`.
- `coverage_log` should **diversify to non-US** (Europe/Asia) as world cities warm.
  As of merge it has 1 entry (a US event); non-US heat is pending warm-up.
- If `coverage_log` is still ~100% US after the cache has warmed (cached_count high),
  that's a real signal — Open-Meteo's non-US heat is being observed but scored/triaged
  out, not a cache bug.
- **Note from the interim window:** even the interim's 10-city fetch showed `readings:3
  failures:7` on some runs — worth confirming #338's warm path does better (the budget +
  429-handling should). Watch `warm_failures` / `forecast_failures` in the metrics.

## Deferred cosmetic follow-ups (from the Opus final review — none blocking)
- `country_forecast_read` is not threaded, so the `forecast_read` **fact** on country
  records is a placeholder (`== cached`); the coverage-floor **gate** is correct.
- Add a day-ceiling-isolating `OpenMeteoBudget` test (current test trips the hour ceiling).
- Two docstrings (`detect_country_records` cached-vs-forecast_read; `_run_world_cached_half`).
- `select_world_budget_cities` is now unused by the orchestrator (kept + unit-tested);
  could be removed.

## Still waiting on Andrew (unchanged from 06-25)
1. **`SELFHEAL_PAT`** ([#309](https://github.com/andrewzp/theheat/issues/309)) — classic PAT (`repo`+`workflow`,
   long expiry) → `gh secret set SELFHEAL_PAT --repo andrewzp/theheat`. Then dispatch
   `workflow-self-heal`, confirm the `SELFHEAL_BEACON` repo variable writes + the
   dashboard self-heal dot greens.
2. **Claim/warrant** ([#324](https://github.com/andrewzp/theheat/pull/324), open, docs-only) — review + 2
   tunables, then EXECUTE `docs/plans/2026-06-22-claim-warrant-model-implementation-plan.md`.
3. **"Recently posted" dashboard feed** — reviewed spec parked on branch
   `design/dashboard-recently-posted-feed`. Say go → TDD build.

## Process notes
- **CI runs `ruff check src/` AND `mypy src/`.** The SDD implementers ran ruff but not
  mypy → one CI failure (a `dict[str,int]` indexed by `Any | None` country key); fixed
  by guarding `if co:`. Future plans' Global Constraints should list the FULL CI gate
  (ruff + mypy), not just ruff.
- **`cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH`
  on EVERY Bash command** — background shells reset cwd (bit me twice this session).
- **2× codex review per artifact earned its keep again**: codex `exec -s read-only`
  (high effort) on the spec found 2 P0 + 4 P1, on the plan another 2 P0 + 4 P1 — all
  real (concurrent-merge clobber, shared-budget reality, anti-re-fire, country bootstrap,
  a genuine mean-count bug). Both single-model self-reviews had declared each artifact ready.
- **Subagent-driven development worked cleanly**: 11 tasks, Sonnet implementers + Sonnet
  reviewers, Opus reviews on the merge/budget/wiring tasks + an Opus whole-branch review.
  Ledger at `.git/sdd/progress.md`. Where the plan abbreviated code (evaluate_city,
  OpenMeteoBudget, classify_world_status, the Task 11 wiring), the controller supplied a
  complete reference into the task brief so implementers transcribed rather than guessed.
