# Handoff — 2026-07-06 (evening)

**`main` @ `5f29035`**, version **0.9.81.0** (changes under CHANGELOG `[Unreleased]`).
Supersedes [docs/handoffs/2026-07-06.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-06.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ Program plan:** [docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md)
> — queue rows 1–7 BUILT AND MERGED. Rows remaining: E1 (voice-floor, fires), R4 (SELFHEAL_PAT, Andrew), A3 (v2, gated on gap-flag evidence).
> **★ Bet A spec:** [docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md).

## TL;DR

Bet A's two editorial consumers shipped DARK: **A1 enrich (#371)** and **A2 boost
(#373)**, each codex-xhigh-looped 4 rounds to a clean APPROVE (every BLOCK finding
real — 10 total across both loops, all fixed same-session). The A1 live dispatch
verify ran GREEN (run 28819095290): the writer attributed and self-reported
`cited_impact`, fact-check killed two identity-mismatched candidates (correct — the
harness bypasses the matcher), the clean one passed the critic, and decision-4
forced `manual_only`. The whole Bet A machine is now merged and waiting on THREE
repo variables, all Andrew's, in spec order: master → enrich → boost.

Also: **#372** — the gpm rolling-accumulation lane presents its static 300mm
threshold as "the previous 7-day record" (a false-record generator upstream of every
gate); the Barrow + Astana pending drafts are affected and should be REJECTED. The
autoship watch item resolved as working-as-designed (GHCN observation lag > the 36h
source-age ceiling — tune `THEHEAT_AUTOSHIP_MAX_AGE_H` or accept manual for lagged
sources).

## What shipped (all merged)

- **A1 — enrich (#371):** `src/editorial/newsworthiness.py` matcher (kind families;
  country; US state agreement both sides, FIRMS via centroid-resolved
  `lat_lon_to_state`; named fire news requires a same-named candidate; nameless fire
  news matching >1 attaches to NONE; heat mortality keeps highest-score ambiguity) →
  `human_impact` on bundles at the triage drain. `IMPACT_GUIDANCE` rides the USER
  prompt only (cache byte-identity); writer JSON gains `cited_impact`. Fact-check
  rule (k) "no warrant, no claim" (bundle-fact impact — GDACS `population_affected`
  — unchanged). Evidence-contract errors on unwarranted entries. **Decision 4** in
  `save_draft`: cited-impact drafts forced `manual_only` incl. the #352 allowlist,
  two-signal fail-closed (writer flag + deterministic sweep: source names, values
  ≥100 w/ comma tolerance, string/money literal+magnitude, casualty/damage stems).
  `news-enrich-dryrun.yml` (workflow_dispatch) is the pre-flip gate — NO `|| true`;
  missing keys or 0 shipping candidates = RED.
- **A2 — boost (#373):** `plan_fire_boosts` (batch per runner) +
  `apply_newsworthiness_boost` (+8 flat; floor threshold−8; ≥1 structured/verified
  entry; rescue-only — passing scores untouched; frozen-`replace`, never mutates).
  **Namespace partition:** nameless events ↔ FIRMS hotspots, named events ↔ named
  NIFC crossings (nameless crossings excluded from planning) — one event can never
  rescue two fires across runners. `news_boost=` provenance rides `score.reasons`
  and now survives downstream kills (object AND serialized-dict scores in
  `_record_downstream_suppression`). Fire-only in v1 (firms + nifc seams,
  error-degrading).
- **#372 filed:** gpm `_detect_rolling_accumulations` writes
  `previous_record_mm=threshold_mm` — the writer honestly renders a static trigger
  as an archive record; no downstream gate can catch it (tweet matches bundle).
  Barrow 498.8mm/7d (~4× its annual) + three simultaneous Kazakh ~year-in-a-week
  totals also look like IMERG artifacts. Fix = distinct alert-threshold fields +
  plausibility guard; claim/warrant (#324) poster child.

## Watch item resolved — autoship vs GHCN lag (no code change)

Both stuck `all_time_high` drafts: critic PASS, armed by #352 at save, then the
posting-time freshness guard demoted them (`post_error: "Autoship blocked: stale
framing (queue or source age)"`) — their `tweet_date`s (Jul 2 / Jun 30) were 2–4
days old at detection, GHCN's normal lag, always > the 36h source-age ceiling.
Working as designed; consequence: GHCN-sourced records structurally land in manual
review. Andrew's call: `gh variable set THEHEAT_AUTOSHIP_MAX_AGE_H --body 96` (or
similar) vs. accepting manual for lagged sources.

## Queue-vs-world review (Andrew asked mid-session)

Verdict: NOT representative. World front page Jul 4–6: US heat dome (200M+ under
alerts, ~25 deaths), Colorado's Aspen Acres fire (87k acres, 8th largest in CO
history, 11k evacuated, CA sending crews), Europe's 1,300+ heat deaths (WHO), 
typhoons Maysak (China, 5 dead) + Bavi (Guam). The 8-draft queue: two small station
records (heat-dome shrapnel), a Basrah near-dup pair (#346 class), two likely-FALSE
precip records (#372), one evergreen air-quality item. Zero fire, zero cyclone
drafts. This is the Congo-vs-Colorado failure at queue scale — and the machinery
that closes it is now fully merged, one flag flip away: A0's news-gap watch would
have flagged the fires, the Europe deaths, and likely both typhoons THIS WEEK.

## ⭐ NEXT SESSION

1. **Rollout watching, spec order** (each step live-verified, one-flip rollback):
   after Andrew flips master — watch 2–3 cycles: `newsworthiness` source-health
   row, `state["news_events"]` citations (hand-check 2–3 URLs), news-gap issue
   sanity. Then enrich: re-run `news-enrich-dryrun` green → flip → impact-carrying
   drafts appear ONLY in manual review. Then boost: flip → watch the suppression
   ledger for `news_boost=` reasons on rescued/killed fires.
2. **E1 — voice-floor iteration on fires** (plan row 8): the reganom four-moves
   pattern + the A1 impact citation; extend the dryrun-harness pattern.
3. **#372 fix**: alert-threshold fields distinct from record fields + plausibility
   guard on rolling accumulations.
4. Held: #346 dup-city (Basrah pair is a live specimen), #324 claim/warrant.

## ACTIONS FOR ANDREW

1. **Reject the Barrow + Astana precip drafts** on the dashboard (#372 — false
   records; do not post).
2. **Flip the master:** `gh variable set THEHEAT_NEWSWORTHINESS_ENABLED --body 1
   --repo andrewzp/theheat` (A0: zero editorial surface, ≤5 Gemini calls/cycle).
   Enrich/boost flips come AFTER A0 verifies live, in spec order.
3. **Decide the autoship ceiling** for lagged sources (`THEHEAT_AUTOSHIP_MAX_AGE_H`,
   default 36; GHCN records need ~96 to ever auto-ship).
4. Standing: `SELFHEAL_PAT` (#309); claim/warrant #324 review; #346 direction.

## Standing rules (unchanged from 2026-07-06 morning, plus one)

- `cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH`
  on EVERY Bash command; Python `.venv/bin/python`; repo andrewzp/theheat.
- CI `test` = ruff + mypy + pytest + dashboard build; run ruff + mypy AND the local
  canary (`THEHEAT_TIME_TRAVEL_DAYS=90 … pytest -q`) before pushing.
- codex-xhigh looped to clean APPROVE on gate/posting/state/storage diffs — the two
  A-phase loops each took 4 rounds and every finding was real; budget for it.
- **NEW:** the LAST codex round must start after the LAST code edit (a background
  reviewer samples a moving tree; its APPROVE covers only what it finished on).
- Never weaken honesty gates; every impact figure needs source+url+as_of.

---

## Paste-ready kickoff prompt for a fresh session

> Pick up @theheat (`cd /Users/andrewpuschel/Documents/Claude/theheat`;
> `PATH=/opt/homebrew/bin:$PATH` + that `cd` on EVERY Bash command; Python
> `.venv/bin/python`; repo andrewzp/theheat). `main` is `5f29035`+, version
> 0.9.81.0. **Read `docs/handoffs/2026-07-06-evening.md` FIRST**, then the program
> plan and Bet A spec it links.
>
> **Context:** three-pillar rows 1–7 are MERGED. Bet A is fully built and DARK:
> A0 (#366) + A1 enrich (#371) + A2 boost (#373), behind
> `THEHEAT_NEWSWORTHINESS_ENABLED` (master) → `THEHEAT_NEWS_ENRICH_ENABLED` →
> `THEHEAT_NEWS_BOOST_ENABLED`, all unset=0, consumers require the master.
>
> **Your main work, in order:**
> 1. Verify live state (fresh git log origin/main, PRs/issues, `gh variable list`).
>    For each Bet A flag Andrew HAS flipped, run its live verify (handoff §NEXT
>    SESSION item 1: A0 = news_events citations + news-gap sanity; A1 = dryrun
>    re-run green + forced-manual drafts; A2 = news_boost reasons in the ledger).
>    One-flip rollback if anything smells. Surface any flag still pending.
> 2. If #372 is unfixed: fix the gpm false-record class (alert-threshold fields
>    distinct from record fields + plausibility guard; codex loop — it touches a
>    bundle contract). Check whether the Barrow/Astana drafts were rejected.
> 3. Then E1 — voice-floor iteration on fires (plan row 8): four-moves fire prompt
>    section + fact-check pairing + A1 impact citation; extend the dryrun harness;
>    codex mandatory.
>
> **Standing rules:** CI test = ruff + mypy + pytest + dashboard build; local canary
> (`THEHEAT_TIME_TRAVEL_DAYS=90 .venv/bin/python -m pytest -q`) before pushing;
> fixture dates today-relative. codex-xhigh (`< /dev/null`, background, ONE
> backgrounding layer, looped to clean APPROVE, last round after last edit) on
> anything touching editorial gates / posting / state / storage. Stage only your
> files; docs as their own PR. Andrew never merges — `gh pr checks <N> --watch`
> then squash-merge on green. Never weaken the honesty gates; every impact figure
> needs source+url+as_of. Work autonomously; stop only for a real fork or a prod
> flag flip (the classifier owns those).
