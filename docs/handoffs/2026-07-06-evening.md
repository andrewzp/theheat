# Handoff — 2026-07-06 (evening, final)

**`main` @ `076eabe`**, version **0.9.81.0** (changes under CHANGELOG `[Unreleased]`).
Supersedes [docs/handoffs/2026-07-06.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-06.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ Program plan:** [docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md)
> — queue rows 1–7 BUILT AND MERGED. Remaining: E1 (voice-floor, fires), R4
> (SELFHEAL_PAT, Andrew), A3 (v2, gated on gap-flag evidence).
> **★ Bet A spec:** [docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md).

## TL;DR

**Bet A is fully built and DARK.** All three phases merged this session's arc —
A0 (#366, prior session), **A1 enrich (#371)**, **A2 boost (#373)** — each editorial
phase codex-xhigh-looped 4 rounds to a clean APPROVE (every BLOCK finding real). The
A1 live dispatch verify ran GREEN (run 28819095290). The whole machine waits on three
repo variables, all Andrew's, in spec order: **master → enrich → boost**.

Also this session: **#372 gpm false-record class FIXED AND MERGED (#376)** — root-caused
to a stuck IMERG sensor at Barrow (identical 71.25 mm for 8 days). Queue swept clean
(`reject-all-drafts`). **#375 filed** — the cyclone "sustained land-threat" gap that left
Super Typhoon Bavi undraftable at peak. And a **Sonnet 5 writer-model evaluation, PARKED**
(see below — do NOT re-investigate; it's a decision, not a loose end).

## What shipped (all merged)

- **A1 — enrich ([#371](https://github.com/andrewzp/theheat/pull/371)):**
  `src/editorial/newsworthiness.py` matcher (kind families; country; US state
  agreement both sides, FIRMS via centroid-resolved `lat_lon_to_state`; named fire
  news requires a same-named candidate; nameless fire news matching >1 attaches to
  NONE; heat mortality keeps highest-score ambiguity) → `human_impact` on bundles at
  the triage drain. `IMPACT_GUIDANCE` rides the USER prompt only (cache byte-identity);
  writer JSON gains `cited_impact`. Fact-check rule (k) "no warrant, no claim"
  (ordinary bundle-fact impact — GDACS `population_affected` — unchanged).
  Evidence-contract errors on unwarranted entries. **Decision 4** in `save_draft`:
  cited-impact drafts forced `manual_only` incl. the #352 allowlist, two-signal
  fail-closed (writer flag + deterministic sweep: source names, values ≥100 w/ comma
  tolerance, string/money literal+magnitude, casualty/damage stems).
  `news-enrich-dryrun.yml` (workflow_dispatch) is the pre-flip gate — NO `|| true`;
  missing keys or 0 shipping candidates = RED.
- **A2 — boost ([#373](https://github.com/andrewzp/theheat/pull/373)):**
  `plan_fire_boosts` (batch per runner) + `apply_newsworthiness_boost` (+8 flat; floor
  threshold−8; ≥1 structured/verified entry; rescue-only — passing scores untouched;
  frozen-`replace`, never mutates). **Namespace partition:** nameless events ↔ FIRMS
  hotspots, named events ↔ named NIFC crossings (nameless crossings excluded from
  planning) — one event can never rescue two fires across runners. `news_boost=`
  provenance rides `score.reasons` and survives downstream kills (object AND
  serialized-dict scores in `_record_downstream_suppression`). Fire-only in v1.
- **#372 gpm fix ([#376](https://github.com/andrewzp/theheat/pull/376)) — the two
  deterministic halves:** (1) `_detect_rolling_accumulations` no longer writes its
  static trigger into the record fields — `alert_threshold_mm` is a distinct field, so
  "above the previous 7-day record of 300.0 mm" is unconstructable from the bundle;
  (2) a **stuck-sensor guard** skips any accumulation window of identical rounded daily
  values. Root cause: Barrow's IMERG cell logged an identical **71.25 mm for eight days**
  while ~5.6 mm actually fell (independent Open-Meteo check) — constant × window length
  is every Barrow draft since June 7, **one of which POSTED 2026-06-07**. codex-xhigh
  2 rounds → clean APPROVE. Remainder (below) is Andrew's coverage call, not a bug.

## Watch item resolved — autoship vs GHCN lag (no code change)

Both stuck `all_time_high` drafts: critic PASS, armed by #352 at save, then the
posting-time freshness guard demoted them (`post_error: "Autoship blocked: stale
framing (queue or source age)"`) — their `tweet_date`s (Jul 2 / Jun 30) were 2–4 days
old at detection, GHCN's normal lag, always > the 36h source-age ceiling. Working as
designed; consequence: GHCN-sourced records structurally land in manual review.
Andrew's call: `gh variable set THEHEAT_AUTOSHIP_MAX_AGE_H --body 96` (or similar) vs.
accepting manual for lagged sources.

## Queue-vs-world review (Andrew asked mid-session)

Verdict: NOT representative. World front page Jul 4–6: US heat dome (200M+ under
alerts, ~25 deaths), Colorado's Aspen Acres fire (87k acres, 8th largest in CO history,
11k evacuated), Europe's 1,300+ heat deaths (WHO), typhoons Maysak (China, 5 dead) +
Bavi (Guam). The 8-draft queue: two small station records, a Basrah near-dup pair (#346
class), two likely-FALSE precip records (#372), one evergreen air-quality item. Zero
fire, zero cyclone drafts. The Congo-vs-Colorado failure at queue scale — and the
machine that closes it is now fully merged, one flag flip away.

## Bavi coverage (#375) — the postable hand-draft

The cyclone lane detected Bavi's whole intensification arc (5 drafts Jul 3–4, all died
unreviewed), then had **no event class** for a sustained Cat-4+ system bearing down on
land — so it went silent at peak newsworthiness. [#375](https://github.com/andrewzp/theheat/issues/375)
is the sensor-side fix (`cyclone_land_threat`); A0's news-gap watch is the detector.

Interim, a gate-checked hand-draft from live JTWC advisories 023/024 (269 chars, safety
PASS — every figure verbatim from the feed). **Andrew to post by hand if it reads right:**

> Typhoon Bavi went from Category 1 to Category 5 in 24 hours on July 3. Three days
> later it is still a super typhoon, pushing 54-foot seas toward Taiwan — over water so
> warm, so deep, that the churning which normally cools a typhoon keeps bringing up
> warm water instead.

(Editorial note for future voice work: the winning move was leading with the astounding
stat — 1→5 in a day — and stating the mechanism in plain language. An earlier attempt
buried the stat; another used a hollow metaphor closer ("an ocean with no brakes") that
deleted the physics. Lead with the "wait, what?"; never poetry without a mechanism.)

## Parked — Sonnet 5 writer-model evaluation (do NOT re-investigate; it's Andrew's call)

Evaluated `claude-sonnet-5` vs the current writer `claude-sonnet-4-6`
(`src/config.py:49`, `THEHEAT_WRITER_MODEL` default). **Paused before any change; no code
touched.** The substance, so it isn't re-derived:

- **The writer call is already migration-clean.** `_call_anthropic` sets no `thinking`,
  no `temperature`/`top_p` — so Sonnet 5's two 400-ing breaking changes don't apply.
- **The one live gotcha:** on 4.6, omitting `thinking` = OFF; on Sonnet 5, omitting =
  adaptive thinking ON, sharing the `max_tokens=1024` budget across 2 writer samples —
  truncation risk. **Hygiene step before any comparison: pin `thinking={"type":"disabled"}`
  in the writer** (good on 4.6 too; makes 4.6↔5 apples-to-apples).
- **Cost:** per-token price identical at standard rates ($3/$15); Sonnet 5 intro $2/$10
  through 2026-08-31; new tokenizer ~30% more tokens for the same text (net ≈ flat now,
  ~30% pricier after Aug 31 — muted for @theheat since the ~5,700-tok system prompt is
  cache-read at ~0.1×). Verify live pricing before any flip.
- **Not a no-deploy flip:** `THEHEAT_WRITER_MODEL` is NOT yet threaded through `bot.yml`
  — flipping needs a one-line passthrough (like the other flags) + the repo var.
- **Recommendation if resumed:** pin thinking-disabled → run both models through the
  dryrun harnesses on the same fixtures → shadow-A/B (`THEHEAT_SHADOW_AB_ENABLED`) with
  the critic + voice-regression grading → flip only if Sonnet 5 wins. Taste + prod call.

## ⭐ NEXT SESSION

1. **Rollout watching, spec order** (each step live-verified, one-flip rollback):
   after Andrew flips **master** — watch 2–3 cycles: `newsworthiness` source-health
   row, `state["news_events"]` citations (hand-check 2–3 URLs), news-gap issue sanity.
   Then **enrich**: re-run `news-enrich-dryrun` green → flip → impact-carrying drafts
   appear ONLY in manual review. Then **boost**: flip → watch the suppression ledger for
   `news_boost=` reasons on rescued/killed fires.
2. **E1 — voice-floor iteration on fires** (plan row 8): the reganom four-moves pattern
   + the A1 impact citation; extend the dryrun-harness pattern. codex mandatory.
3. **#375 — `cyclone_land_threat` event class** (forecast track within N NM of a named
   landmass → one `manual_only` draftable event per storm-landmass pair); Bavi's
   Taiwan/China approach is the live test case this very week.
4. **#372 remainder** (Andrew's coverage calls, NOT re-fixing the merged halves):
   daily-record-path stuck guard, cross-source witness plausibility, ≤60° latitude band.
5. Held: #346 dup-city (the Basrah pair was a live specimen), #324 claim/warrant.

## ACTIONS FOR ANDREW

1. **Reject the pending Barrow precip draft** (the post-sweep alerts run re-minted one
   before #376 landed; #376 stops future regeneration) and **decide on the POSTED
   2026-06-07 Barrow tweet** (213.8 mm = 3 × the stuck sensor value; recommendation:
   delete).
2. **Post the Bavi hand-draft** above if it reads right (all figures verbatim from JTWC).
3. **Flip the master:** `gh variable set THEHEAT_NEWSWORTHINESS_ENABLED --body 1 --repo
   andrewzp/theheat` (A0: zero editorial surface, ≤5 Gemini calls/cycle). Enrich/boost
   flips come AFTER A0 verifies live, in spec order.
4. **Decide the autoship ceiling** for lagged sources (`THEHEAT_AUTOSHIP_MAX_AGE_H`,
   default 36; GHCN records need ~96 to ever auto-ship).
5. Standing: `SELFHEAL_PAT` (#309); claim/warrant #324 review; #346 direction.

## Standing rules (unchanged from 2026-07-06 morning, plus one)

- `cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH` on
  EVERY Bash command; Python `.venv/bin/python`; repo andrewzp/theheat.
- CI `test` = ruff + mypy + pytest + dashboard build; run ruff + mypy AND the local
  canary (`THEHEAT_TIME_TRAVEL_DAYS=90 … pytest -q`) before pushing.
- codex-xhigh looped to clean APPROVE on gate/posting/state/storage diffs — the two
  A-phase loops each took 4 rounds and every finding was real; budget for it.
- **The LAST codex round must start after the LAST code edit** (a background reviewer
  samples a moving tree; its APPROVE covers only what it finished on). ONE backgrounding
  layer per codex run (no `&` inside a run_in_background command). Merge chains must
  verify end-state (a squash commit on origin/main), not `gh pr checks --watch` exit
  codes — `--watch` can exit green before checks register.
- Never weaken honesty gates; every impact figure needs source+url+as_of.

---

## Paste-ready kickoff prompt for a fresh session

> Pick up @theheat (`cd /Users/andrewpuschel/Documents/Claude/theheat`;
> `PATH=/opt/homebrew/bin:$PATH` + that `cd` on EVERY Bash command; Python
> `.venv/bin/python`; repo andrewzp/theheat). `main` is `076eabe`+, version 0.9.81.0.
> **Read `docs/handoffs/2026-07-06-evening.md` FIRST**, then the program plan and Bet A
> spec it links.
>
> **Context:** three-pillar rows 1–7 are MERGED. Bet A is fully built and DARK: A0
> (#366) + A1 enrich (#371) + A2 boost (#373), behind `THEHEAT_NEWSWORTHINESS_ENABLED`
> (master) → `THEHEAT_NEWS_ENRICH_ENABLED` → `THEHEAT_NEWS_BOOST_ENABLED`, all unset=0,
> consumers require the master. #372's gpm false-record class is FIXED (#376) — do not
> re-fix it; only its remainder (daily-path guard, witness plausibility, ≤60° band) is
> open, and that's Andrew's coverage call. The Sonnet 5 writer swap was evaluated and
> PARKED (handoff §Parked) — do not re-investigate unless Andrew asks.
>
> **Your main work, in order:**
> 1. Verify live state (fresh `git log origin/main`, open PRs/issues, `gh variable
>    list`). For each Bet A flag Andrew HAS flipped, run its live verify (handoff §NEXT
>    SESSION item 1: A0 = news_events citations + news-gap sanity; A1 = dryrun re-run
>    green + forced-manual drafts; A2 = news_boost reasons in the ledger). One-flip
>    rollback if anything smells. Surface any flag still pending.
> 2. **E1 — voice-floor iteration on fires** (plan row 8): four-moves fire prompt
>    section + fact-check pairing + the A1 impact citation; extend the dryrun harness;
>    codex mandatory. (This is the richest test of A1's impact-carrying fire tweets.)
> 3. **#375 — `cyclone_land_threat`** event class if E1 stalls or Andrew prioritizes
>    it: forecast track within N NM of a named landmass → one manual_only draftable
>    event per storm-landmass pair. codex mandatory (new state/editorial surface).
>
> **Standing rules:** CI test = ruff + mypy + pytest + dashboard build; local canary
> (`THEHEAT_TIME_TRAVEL_DAYS=90 .venv/bin/python -m pytest -q`) before pushing; fixture
> dates today-relative. codex-xhigh (`< /dev/null`, background, ONE backgrounding layer,
> looped to clean APPROVE, LAST round after LAST edit) on anything touching editorial
> gates / posting / state / storage. Keep Python↔JS mirrors + allowlists in sync. Stage
> only your files; docs as their own PR. Andrew never merges — verify the squash landed
> on origin/main (don't trust `gh pr checks --watch` exit alone), then squash-merge on
> green. Never weaken the honesty gates; every impact figure needs source+url+as_of.
> Work autonomously; stop only for a real fork or a prod flag flip (the classifier owns
> those).
