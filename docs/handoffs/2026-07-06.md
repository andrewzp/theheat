# Handoff — 2026-07-06

**`main` @ `8440f3a`**, version **0.9.81.0** (changes accumulate under CHANGELOG
`[Unreleased]`). Supersedes
[docs/handoffs/2026-07-03.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-03.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ The program plan (EXECUTING):**
> [docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md)
> — queue rows 1–5 are BUILT AND MERGED; next up is **A1 (enrich)**, which is also
> Pillar 1's sourced-anecdotes payoff.
> **★ The Bet A design (Andrew-approved):**
> [docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md).

## TL;DR

The 2026-07-03/04 session executed the whole VISION.md mandate and then, on Andrew's
"follow your own recommendations and keep going," the plan's PR queue in order. Seven
PRs merged: **#361** reganom stakes fix · **#362 writer watch** · **#363** plan+spec
docs · **#364 queue watch** · **#365 time-travel canary** · **#366 Bet A phase 0
(shipped DARK)** · **#367** changelog. Everything codex-xhigh-looped to clean APPROVE
(A0's round 1 was a BLOCK with a real P0 — fixed). The suite is proven green at
**+0/+90/+365 days** (2,161 tests).

**Live since (verified 2026-07-06):** the canary's first scheduled run is GREEN; the
bot is drafting all weekend (runs green); the **queue watch fired for real** —
[issue #368](https://github.com/andrewzp/theheat/issues/368): **15 pending drafts,
oldest ≈61h** (3 precipitation_extreme, 3 absolute_extreme, 3 dust_event,
2 all_time_high, 2 fire, 1 cyclone_rapid_intensification, 1 air_quality_hazard).
**`THEHEAT_NEWSWORTHINESS_ENABLED` is still unset** — Bet A phase 0 is merged but
dark; activation is Andrew's one command.

## What shipped (all merged)

- **Writer watch (#362)** — `budget_exhausted` kills in the last 24h (bot drafting)
  → loud auto-closing issue, `ours` label; JS mirror in the source-health payload.
  The 2026-07-03 credits incident class can no longer be silent.
- **Queue watch (#364)** — pending human-gated drafts >24h → loud issue with
  count/oldest/type breakdown. Load-bearing subtlety (codex, twice): human-gated is
  classified by RUNNABLE state — auto-owned means `auto_approve_at` present AND
  `approval_mode` ∈ {`auto`, `policy_auto`}; `approval_policy.mode` is only the
  recommendation, so failed-closed armed_auto drafts count.
- **Time-travel canary (#365)** — `THEHEAT_TIME_TRAVEL_DAYS=<n>` shifts the whole
  suite's clock (freezegun, from `pytest_configure`, tick=True); weekly workflow runs
  +30d/+365d; registered as the FIFTH workflow in workflow-health + the automation.js
  mirror + self-heal runbook. The first +90d run found **five live calendar bombs**
  (enso seasonal fixture, ice_mass 11 days from detonation, methane, **NHC — which
  actually detonated on #364's CI mid-session at UTC midnight**, open_meteo coverage
  prune) — all fixtures now today-relative. **New standing rule: run
  `THEHEAT_TIME_TRAVEL_DAYS=90 .venv/bin/python -m pytest -q` before pushing any
  PR that touches tests/fixtures.**
- **Bet A phase 0 (#366, DARK)** — `src/data/newsworthiness.py`: NIFC leg with a
  **live-verified WFIGS field contract** (TotalIncidentPersonnel/IncidentSize/cost;
  NO fatality fields exist there — fatalities can only arrive via grounded search) +
  one Gemini `google_search` call whose events are **verified-or-dropped per impact
  entry** (independent URL fetch + separate Flash check, ≤3 fetches/cycle, dead-URL
  cache); deterministic floor drops any entry missing source/url/as_of. State:
  `news_events` (7d, window-clamped both directions) + `candidates_log` (recorded at
  `_enqueue_story_candidate`, 7d) — MERGE_SPEC + BotState + **sqlite
  `_METADATA_JSON_KEYS`** (codex caught sqlite silently dropping new keys). Sentinel
  **news-gap watch**: recent structured/verified world events with no
  boundary-guarded match in candidates/pending/posts → advisory issue (the
  European-heat-deaths miss-detector). Runner is a normal flag-gated source row
  (skipped/degraded/failed) — silent failure structurally impossible.

## ⭐ NEXT SESSION — the main work

1. **A0 live verification** (needs Andrew's flag flip first — see his actions): after
   `THEHEAT_NEWSWORTHINESS_ENABLED=1`, watch 2–3 cycles: `newsworthiness` source-health
   row success/degraded, `state["news_events"]` populating with REAL citations
   (hand-check 2–3 URLs), news-gap issue behavior sane (no false flood). One-flip
   rollback if anything smells.
2. **A1 — enrich** (spec §5; Pillar 1's anecdote payoff), behind
   `THEHEAT_NEWS_ENRICH_ENABLED` default OFF: match verified news events to the
   cycle's candidates at the triage drain (in-memory); attach
   `human_impact: [ImpactFact]` to the matched StoryBundle; writer-prompt section
   (cite ONLY from `bundle.human_impact`, ONLY with attribution, verbatim-or-rounded,
   past tense + as-of); fact-check rule KILLING any impact claim without a matching
   entry; evidence-contract gate (entries missing source/url/as_of block the writer);
   **forced `manual_only` by content** in `save_draft` (`cited_impact` writer-JSON
   field + regex cross-check — decision 4). Extend the dispatch harness
   (reganom-dryrun pattern) with a fire + `human_impact` fixture and live-verify
   before any flip. codex-xhigh mandatory (editorial gates).
3. **A2 — boost** (spec §4): flat +8 rescue at the fire score gate, floor
   threshold−8, source-required, reasons carry provenance. After A1.
4. Watch items: **2 `all_time_high` drafts sat manual >24h** despite #352 autoship —
   check whether the critic failed them (correct fail-closed) or the autoship path
   missed them; #345 `cov:` still climbing; writer/queue watches behaving.

## ACTIONS FOR ANDREW

1. **Triage the 15 aging drafts** ([#368](https://github.com/andrewzp/theheat/issues/368),
   oldest ≈61h) on the dashboard — the TTL sweep auto-rejects at 7d (21d slow), which
   silently discards any good story.
2. **Activate Bet A phase 0** (zero editorial surface, ≤5 Gemini calls/4h cycle, no
   Anthropic):
   `gh variable set THEHEAT_NEWSWORTHINESS_ENABLED --body 1 --repo andrewzp/theheat`
   (The auto-mode classifier correctly refused to let the agent flip a prod flag.)
3. **`SELFHEAL_PAT`** ([#309](https://github.com/andrewzp/theheat/issues/309)) — one
   fine-grained PAT (Actions r/w, Contents r/w, Issues r/w) + `gh secret set
   SELFHEAL_PAT --repo andrewzp/theheat`; arms the already-merged red-workflow
   auto-fixer.
4. Standing: claim/warrant [#324](https://github.com/andrewzp/theheat/pull/324)
   review; [#346](https://github.com/andrewzp/theheat/pull/346) dup-city direction.

## Held / carried

- Yield watch [#308](https://github.com/andrewzp/theheat/issues/308) (pre-existing,
  advisory). UK & Ireland reganom draft (writer-length fix + guard clear). Coverage
  watch live for heat only (extend per-source is Future). Hand-sourced Europe-heat /
  Western-fires drafts in the 2026-07-03 handoff remain postable by hand.

## Standing rules (delta from 2026-07-03: two additions)

- `cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH`
  on EVERY Bash command; Python = `.venv/bin/python`; repo `andrewzp/theheat`.
- CI `test` = ruff + mypy + pytest + dashboard build; run ruff + mypy before pushing.
- **NEW:** run the local canary (`THEHEAT_TIME_TRAVEL_DAYS=90 … pytest`) before
  pushing test-touching PRs; all fixture dates today-relative.
- **NEW:** the sentinel now carries FIVE watches (failing/yield/coverage/writer/queue
  + news-gap) and workflow-health monitors FIVE workflows — keep Python↔JS mirrors
  and both allowlists (`MONITORED_WORKFLOWS`, sqlite `_METADATA_JSON_KEYS`) in sync
  when adding to either.
- codex-xhigh (`< /dev/null`, background, looped to clean APPROVE) on anything
  touching editorial gates / posting / state / storage. Stage only your files; docs
  as their own PR. Andrew never merges — `gh pr checks <N> --watch` then `gh pr merge
  <N> --squash --delete-branch` on green. Never weaken honesty gates. The writer is
  the metered Anthropic API — a low balance now trips the WRITER WATCH instead of
  failing silently.

---

## Paste-ready kickoff prompt for a fresh session

> Pick up @theheat (`cd /Users/andrewpuschel/Documents/Claude/theheat`;
> `PATH=/opt/homebrew/bin:$PATH` + that `cd` on EVERY Bash command; Python
> `.venv/bin/python`; repo andrewzp/theheat). `main` is `8440f3a`, version 0.9.81.0.
> **Read `docs/handoffs/2026-07-06.md` FIRST** (live state + full session record),
> then the program plan `docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md`
> and the approved spec `docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md`.
>
> **Context:** the three-pillar plan is EXECUTING — rows 1–5 merged (#361 reganom fix,
> #362 writer watch, #364 queue watch, #365 time-travel canary, #366 Bet A phase 0
> shipped DARK behind `THEHEAT_NEWSWORTHINESS_ENABLED`, unset=0). The queue watch is
> live and fired (#368).
>
> **Your main work, in order:**
> 1. **Verify live state first** (a parallel session may have moved things): fresh
>    `git log origin/main`, open PRs/issues, repo variables. If Andrew has NOT yet
>    flipped `THEHEAT_NEWSWORTHINESS_ENABLED=1`, surface that it's still pending and
>    proceed to A1 build (it's flag-independent to BUILD; only live verify needs it).
>    If it IS flipped: verify A0 live — `newsworthiness` source-health row,
>    `state["news_events"]` populating with real citations (hand-check 2–3 URLs),
>    news-gap issue behavior. One-flip rollback if wrong.
> 2. **Build A1 — enrich** (spec §5, decision 4; this is ALSO Pillar 1's
>    sourced-anecdotes payoff) behind `THEHEAT_NEWS_ENRICH_ENABLED` default OFF:
>    match verified news_events to the cycle's candidates at the triage drain;
>    attach `human_impact` to matched StoryBundles; writer-prompt section (cite ONLY
>    from bundle.human_impact, ONLY with attribution, past tense + as-of); fact-check
>    KILL rule for unmatched impact claims; evidence-contract gate; **forced
>    manual_only by content** in `save_draft` (`cited_impact` writer-JSON field +
>    regex cross-check). Extend the reganom-dryrun harness pattern with a fire +
>    human_impact fixture; live dispatch verify before any flip.
> 3. **Then A2 — boost** (spec §4): flat +8 rescue at the FIRE score gate only,
>    hard floor threshold−8, ≥1 structured/verified impact entry required,
>    `news_boost` provenance in score.reasons. Behind `THEHEAT_NEWS_BOOST_ENABLED`.
>
> **Watch items:** 2 all_time_high drafts sat manual >24h despite #352 autoship —
> check critic verdicts (fail-closed is correct behavior) vs an autoship-path miss;
> writer/queue watches; canary Mondays.
>
> **Standing rules:** CI `test` = ruff + mypy + pytest + dashboard build; run ruff +
> mypy AND the local canary (`THEHEAT_TIME_TRAVEL_DAYS=90 .venv/bin/python -m pytest
> -q`) before pushing; all fixture dates today-relative. codex-xhigh
> (`model_reasoning_effort="xhigh"`, `< /dev/null`, background, looped to clean
> APPROVE — expect it to find real things; A0's round 1 was a BLOCK) on anything
> touching editorial gates / posting / state / storage. Keep Python↔JS mirrors and
> the allowlists (MONITORED_WORKFLOWS, sqlite `_METADATA_JSON_KEYS`) in sync. Stage
> only your files; docs as their own PR. Andrew never merges — merge on green CI.
> Never weaken the honesty gates; every impact figure needs source+url+as_of (the
> iron constraint). Work autonomously; stop only for a real fork or a prod flag flip
> (the classifier owns those).
