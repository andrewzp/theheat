# Handoff — 2026-07-07 (the audit + program session)

**`main` @ `7a64fb1`**, version **0.9.82.0** (changes under CHANGELOG `[Unreleased]`).
Supersedes [docs/handoffs/2026-07-06-night.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-06-night.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ Program plan (SUCCESSOR, awaiting Andrew — PR [#382](https://github.com/andrewzp/theheat/pull/382)):**
> `docs/superpowers/plans/2026-07-06-front-page-parity-plan.md` + the per-row
> implementation plans in `docs/superpowers/plans/front-page-parity/` — both live on
> branch `docs/front-page-parity-plan` until #382 merges.
> **★ Prior program (COMPLETE):** [docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-07-03-three-pillar-upgrade-plan.md)
> — rows 1–8 ALL MERGED. Remaining loose ends: R4 (SELFHEAL_PAT, Andrew), A3 (now
> row 12 of the successor, evidence-gated).

## TL;DR

Andrew asked the product question — *"are our tweets going to be amazing and
interesting and representative of what's happening around the world?"* — and this
session answered it with a whole-project audit, then built the program to fix it, then
built the program out to per-row implementation plans a zero-context session can
execute.

**The honest answer: not yet, on all three VISION bars.** (1) Representative: the
queue-vs-world review found zero fire and zero cyclone drafts while the world's front
page was a US heat dome, Colorado's 8th-largest fire ever, 1,300+ European heat deaths,
and two typhoons — and the machine that fixes selection (Bet A) is merged, verified, and
**dark: all three flags still unset**. (2) Amazing: the bot's own grading instrument
reads **20% A-rate vs the >50% bar** (Jul 5), with the failure modes named and counted
(P_tier 10 instances/4 types; P_dust 11/11; P_close 16 cycles; P9) — and the corpus
holding that evidence sat 29 cycles unmerged on `daily-plan-current`. (3) Shipping:
good drafts die unreviewed in the manual queue (Bavi's five), and nothing measures the
reader side (the engagement lane is built and dormant).

**Everything produced this session is in [PR #382](https://github.com/andrewzp/theheat/pull/382)
— OPEN, deliberately unmerged: it is Andrew's adoption gate.**

## What's in PR #382 (branch `docs/front-page-parity-plan`)

1. **The program plan** (`2026-07-06-front-page-parity-plan.md`): the audit verdict
   with the instruments' numbers, two managed metrics — **FPP (front-page parity:
   detected/drafted/posted vs the news lane's verified world events, weekly snapshot,
   verticals-scoped denominator)** and **A-rate** — and a 14-row leverage-ordered
   queue across 5 tracks (flips → event-class gaps → voice floor → ship-the-queue →
   measurement). codex-xhigh 4 rounds → clean APPROVE; it caught 3 real P0s in my own
   drafts (dust bundles carry NO WHO anchor — the actual P_dust root cause; Open-Meteo
   `dust` ≠ PM10, so the anchor must be co-measured `pm10`; FPP isn't day-one
   computable — 7-day rolling windows need a weekly snapshot).
2. **Per-row implementation plans** (`front-page-parity/INDEX.md` + track-0 runbook +
   rows 3–14): one self-contained TDD plan per build row, written for a zero-context
   implementer — exact files with verified line numbers, complete code in every step,
   exact commands, per-PR codex-loop instructions. Grounded by three code-survey
   agents; codex-xhigh 3 rounds on the set → clean APPROVE (caught a wrong test-helper
   kwarg, non-self-contained dryrun tasks, and a cyclone enqueue that would have lost
   telemetry attribution). Row 12 (A3) is a GATED design skeleton behind a 4-point
   evidence checklist; row 13 is a read-only spike protocol.

Load-bearing facts locked into those plans (verified against code, do not re-derive):
- Row 5 (#375): JTWC warning text we ALREADY store carries the `FORECASTS:` blocks
  verbatim-unparsed; NHC needs a new TCM fetch via `CurrentStorms.json`; Task 0
  verifies the live contract before any code. Landmass v1 = nearest `cities.csv` place;
  one-shot per (storm, landmass) via a new state key with union merge.
- Row 3: the corpus merge is merge-tree-verified conflict-free (3 docs files); the
  staleness fix is a `tweet_date`-keyed `apply_forecast_elapsed_sweep` scoped to
  `{absolute_extreme, wet_bulb_extreme}` — the TTL sweep only reads `created_at`.
- Row 9: the metrics lane is already general (publish_ledger + posted drafts pool);
  only the bot.yml passthrough for `THEHEAT_METRICS_ENABLED` is missing.
- Rows 4/7/11 add fact-check rules — the plans letter them order-tolerantly (tests pin
  phrases, never letters).

## Live state at handoff

- Bet A flags: ALL THREE still unset (verified at session start). Nothing to
  live-verify yet; the track-0 runbook has every verify + rollback step.
- Open PRs: **#382 (this program — Andrew's review)**, #346 (dup-city, held), #324
  (claim/warrant, held), #207 (daily-plan rolling).
- Open issues: #375 (→ row 5), #372 remainder (Andrew's coverage call), #309
  (SELFHEAL_PAT), #308 (yield watch: nhc / reanalysis_anomaly / usgs_quakes zero-yield
  — advisory, plausibly seasonal).
- Working tree: parked on `docs/front-page-parity-plan` (so the plan links resolve)
  after this sweep's branch dance; see the kickoff prompt's first paragraph.
- Earlier this session (already merged): E1 fire voice floor #379 + workflow-injection
  class-fix #380 + docs #381 — see
  [docs/handoffs/2026-07-06-night.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-06-night.md).

## ACTIONS FOR ANDREW (ordered)

1. **Review PR [#382](https://github.com/andrewzp/theheat/pull/382)** — merging it
   adopts the program; rows execute from the INDEX after that. (Both docs are rendered
   as HTML in /tmp if you prefer Chrome: `2026-07-06-front-page-parity-plan.html`,
   `INDEX.html`.)
2. **Flip the master** when ready:
   `gh variable set THEHEAT_NEWSWORTHINESS_ENABLED --body 1 --repo andrewzp/theheat`
   (zero editorial surface; starts the FPP + A3 evidence clocks). Enrich → boost follow
   per the track-0 runbook.
3. **Reject the pending Barrow precip draft** on the dashboard; decide the posted
   2026-06-07 Barrow tweet (recommendation: delete).
4. **Bavi hand-draft**: post it if still timely (text in the 2026-07-06-evening
   handoff §Bavi) — its window may have passed; check the storm first.
5. Standing: autoship ceiling (`THEHEAT_AUTOSHIP_MAX_AGE_H` 36→96); `SELFHEAL_PAT`
   (#309); #324 review; #346 direction. Row 9 needs one confirmation: the X API tier
   allows ~30 `GET /2/tweets` batched reads/month.

## Standing rules (unchanged, plus two session lessons)

- The full battery lives in the INDEX §Standing rules — every implementer reads it.
- **Session lesson 1:** the `cd /Users/andrewpuschel/Documents/Claude/theheat &&`
  prefix is required on EVERY Bash command *including codex and git* — fresh shells
  lose cwd; the failure signatures are codex "Not inside a trusted directory" and git
  exit 128 "not a git repository." Treat either as the cwd slip, not a real error.
- **Session lesson 2 (this session's codex loops, 12 rounds total):** every single
  BLOCK finding across 5 loops was real. Budget for the loop; never skip the
  last-round-after-last-edit rule.

---

## Paste-ready kickoff prompt for a fresh session

> Pick up @theheat. `cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH`
> — that exact prefix on EVERY Bash command INCLUDING codex and git invocations (fresh
> shells lose cwd; codex then fails "Not inside a trusted directory", git exits 128 —
> treat those as the cwd slip). Python `.venv/bin/python`; repo `andrewzp/theheat`.
> `main` is `7a64fb1`+, version 0.9.82.0. ⚠️ The working tree may be parked on branch
> `docs/front-page-parity-plan` (PR #382's branch — intentional, so Andrew's file links
> resolve; it contains main plus the program docs). Run `git branch --show-current`
> first; STAY on that branch while #382 is open; after #382 merges,
> `git checkout main && git pull` and delete the local branch.
>
> **READ FIRST, in order:** (1) `docs/handoffs/2026-07-07.md` — live state + the full
> session record. (2) `gh pr view 382 --repo andrewzp/theheat --json state,comments,mergedAt`
> — then the program plan `docs/superpowers/plans/2026-07-06-front-page-parity-plan.md`
> and `docs/superpowers/plans/front-page-parity/INDEX.md` (on #382's branch). (3) The
> INDEX §Standing rules — they bind every task you run.
>
> **CONTEXT:** Three-pillar rows 1–8 ALL MERGED (E1 fire voice floor = #379+#380). Bet
> A is fully built and DARK behind `THEHEAT_NEWSWORTHINESS_ENABLED` (master) →
> `THEHEAT_NEWS_ENRICH_ENABLED` → `THEHEAT_NEWS_BOOST_ENABLED`, all unset=0. The audit
> verdict (A-rate 20% vs >50%; queue-vs-world zero fire/cyclone drafts; engagement lane
> dormant) and the 14-row program to fix it are in #382, codex-looped clean. The Sonnet
> 5 writer swap is PARKED (2026-07-06-evening handoff §Parked) — do NOT re-investigate.
> #372's merged halves (#376) — do NOT re-fix; only its remainder is open (Andrew's
> coverage call). #375 is row 5 of the program — do not build it outside its plan.
>
> **YOUR WORK, in order:**
> 1. **Verify live state:** fresh `git log origin/main -5`, `gh pr list`, `gh issue
>    list`, `gh variable list --repo andrewzp/theheat`. For each Bet A flag Andrew HAS
>    flipped, run its live verify per
>    `docs/superpowers/plans/front-page-parity/track-0-runbook.md` (A0: `news_events`
>    citations hand-checked + news-gap issue sanity; A1: `news-enrich-dryrun` GREEN
>    before the flip, impact drafts land manual-only after; A2: `news_boost=`
>    provenance in the suppression ledger). One-flip rollback if anything smells;
>    surface any flag still pending.
> 2. **If #382 is MERGED (or Andrew comments "go"):** execute the program from the
>    INDEX in its stated order — row 3 (PR-A corpus merge, then PR-B forecast-elapsed
>    sweep) and row 4 (P_tier + P_dust) first; then row 5 (#375 — run its Task 0
>    feed-contract verification FIRST, and check whether a warned storm is active as
>    the live test case). Every plan is self-contained: follow it task-by-task
>    (superpowers:subagent-driven-development or superpowers:executing-plans), TDD
>    exactly as written, one PR per plan (or per task where the plan says so),
>    codex-xhigh looped to clean APPROVE wherever the plan marks it mandatory, last
>    round after the last edit.
> 3. **If #382 is OPEN and unreviewed:** do NOT merge it (it is Andrew's adoption
>    gate) and do NOT start its rows. Do item 1's watch duties, handle anything Andrew
>    has explicitly asked for in comments/issues, and surface #382 as the blocking
>    decision in your summary.
>
> **WATCH ITEMS:** pending Barrow precip draft (Andrew rejects on the dashboard); the
> Bavi hand-draft window (check the storm — likely passed); `THEHEAT_AUTOSHIP_MAX_AGE_H`
> decision (36→96; GHCN records structurally never autoship today); #308 yield watch
> (advisory; look only if it persists into a second week); #309 SELFHEAL_PAT (Andrew);
> canary Monday (time-travel suite); #346 + #324 held for Andrew.
>
> **STANDING RULES (non-negotiable):** CI `test` = ruff + mypy + pytest + dashboard
> build. Before EVERY push: `ruff check src/ tests/` AND `mypy src/` AND
> `THEHEAT_TIME_TRAVEL_DAYS=90 .venv/bin/python -m pytest -q` all green; fixture dates
> today-relative, never literals. codex-xhigh =
> `cd /Users/andrewpuschel/Documents/Claude/theheat && codex exec -c model_reasoning_effort='"xhigh"' "<prompt>" < /dev/null`
> run in background, ONE backgrounding layer, looped to clean APPROVE (zero P0/P1/P2),
> LAST round STARTS after the LAST code edit — mandatory on anything touching editorial
> gates / posting / state / storage. Merge chains verify END STATE: checks green →
> squash-merge → `git fetch origin && git log origin/main --oneline -1` shows your PR
> (never trust `--watch` exit alone). Andrew never merges — EXCEPT #382 itself, which
> is his. Stage only files your plan names; VERSION + CHANGELOG ride code PRs;
> docs/handoffs as their own PR. Dispatch-workflow inputs via `INPUT_*` env only. Never
> weaken honesty gates; every impact figure needs source+url+as_of. Keep Python↔JS
> mirrors in sync with mirror tests. Work autonomously; stop only for a real fork or a
> prod flag flip (the classifier owns those).
