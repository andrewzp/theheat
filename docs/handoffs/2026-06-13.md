# Resume @theheat — Next Session Prompt (2026-06-13)

Supersedes [/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-06-11.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-06-11.md). **Production state (the gist `state.json`, the live dashboard, GitHub PRs/issues/variables, the claude.ai Routines UI) outranks this snapshot — read it first when it disagrees.**

---

## TL;DR

**Main at 0.9.62.0 (+ this sweep → 0.9.63.0). The THIRTY-LOOP is COMPLETE and the dashboard is deployed.** This session:

1. **THIRTY-LOOP DONE.** Codex executed all 35 steps of the 30-item audit backlog autonomously (S-01..S-35, PRs [#222](https://github.com/andrewzp/theheat/pull/222)–[#265](https://github.com/andrewzp/theheat/pull/265), self-merged per its kickoff authorization). Only **S-24 is BLOCKED** (`record-margin-percentile` — the thresholds DB stores only the current record value/year, no margin-history distribution; not fixable without a new data pipeline). The bot runs from `main`, so this code has been live throughout. **"The goal seek project" = these 30 fixes** (Andrew's name for the loop; there is no separate project — don't go looking for one).
2. **Dashboard deployed** to Vercel prod (the merged S-03/S-19/S-35 UI work had predated the live site). READY + serving (HTTP 401 auth wall = up).
3. **Two supply flags FLIPPED LIVE:** `THEHEAT_WRITER_SAMPLES=2` + `THEHEAT_CRITIC_REVISE_ENABLED=1` (best-of-2 drafts + one critic-requested rewrite). Effective on the **next scheduled cycle**; all drafts still hit the manual approval queue. Rollback: `gh variable set THEHEAT_WRITER_SAMPLES --body 1` / `THEHEAT_CRITIC_REVISE_ENABLED --body 0 --repo andrewzp/theheat`.
4. **THE ACTIVE NEXT JOB — the SOURCE-REDUNDANCY lane is GREENLIT (Andrew, 2026-06-13).** Execute it via its diligent kickoff: [/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-06-13-source-redundancy-CODEX-KICKOFF.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-06-13-source-redundancy-CODEX-KICKOFF.md) (launch command + verbatim executor prompt) against the plan [/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-06-13-source-redundancy-lane.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-06-13-source-redundancy-lane.md) (R-00..R-09). Token-free Codex execution, one PR per step, per the diligent process (verify the live feed → TDD → implement to spec → gates with pasted evidence → self-review on `[CODEX]` steps → honesty checklist). R-00 (provenance + grades + `with_witness`) is the mandatory foundation; then R-02..R-05 are the independent feeds that actually stop host-outage no-draft days. R-09 (sea ice) STOPs for Andrew on a parser dependency. (PR [#266](https://github.com/andrewzp/theheat/pull/266), which first carried this plan, is superseded by the greenlight PR that landed the plan on `main`; close it.)

**Root-cause framing (load-bearing — re-state it before anyone over-invests in feeds):** the 7-day funnel was **924,425 observed → 26,161 promoted → 6 drafted**; triage caps killed **135** candidates vs **65** by all quality gates combined. So **most no-draft days are editorial SUPPLY, not outages** — which is exactly why the supply flags (multi-draft, the still-dark `MAX_DRAFTS`/reganom) are the highest-leverage levers, and why the redundancy lane only addresses the *outage subset*.

---

## Production state (verified 2026-06-13)

- **Version:** `main` 0.9.62.0 (this sweep PR bumps to 0.9.63.0; note [#266](https://github.com/andrewzp/theheat/pull/266) also claims 0.9.63.0 on its parked branch — whichever merges second rebumps).
- **Workflows ACTIVE (do not disable):** `theheat-bot`, `refresh-thresholds`, `voice-regression`, `source-health-sentinel`.
- **Source-health: CLEAN** — zero open sentinel issues.
- **Repo variables set:** `THEHEAT_GPM_SOURCE=datapool`, `THEHEAT_WRITER_SAMPLES=2`, `THEHEAT_CRITIC_REVISE_ENABLED=1`. (`ROUTINE_BEACON` exists but is a DEAD canary.)
- **reganom DORMANT** (`THEHEAT_REGANOM_ENABLED` unset). **Posting `manual_only`.**
- **Grading routine:** judge health by the `daily-plan-current` PR [#207](https://github.com/andrewzp/theheat/pull/207) / `docs/QUALITY_TREND.md` freshness — NEVER `ROUTINE_BEACON`. #207 is the routine's rolling PR; **never touch it.**
- **Open PRs:** only #207 (untouchable) and #266 (parked redundancy plan).

## Feature-flag status — what is LIVE vs DARK

| Flag (repo variable) | State | Effect | How to flip |
|---|---|---|---|
| `THEHEAT_WRITER_SAMPLES` | **LIVE =2** | best-of-2 drafts | `gh variable set … --body N` |
| `THEHEAT_CRITIC_REVISE_ENABLED` | **LIVE =1** | one critic rewrite vs kill | `… --body 0/1` |
| `THEHEAT_MAX_DRAFTS_PER_CYCLE` | dark (3) | more candidates/cycle (supply) | one-liner; passthrough exists |
| `THEHEAT_REGANOM_ENABLED` | dark (0) | new regional-anomaly event class | one-liner; runbook `docs/runbooks/reganom-activation.md` (S-28) |
| `THEHEAT_CONCURRENT_SOURCES` | dark (0) | parallel source fetch (faster cycles) | one-liner; passthrough exists |
| `THEHEAT_ENGAGEMENT_WINDOW_ENABLED` | dark (0) | defer dead-hour auto-publish (no effect while manual_only) | one-liner |
| `THEHEAT_HOT10_CARD_ENABLED` | dark | image card on Hot10 tweets | needs a bot.yml env passthrough FIRST, then the var |
| `THEHEAT_METRICS_ENABLED` | dark | ingest tweet engagement metrics | needs a bot.yml passthrough + an X-API-tier check |
| `OPENAQ_API_KEY` (secret) | unset | AQ ground-station corroboration (S-25) | `gh secret set OPENAQ_API_KEY` + bot.yml passthrough |

**Note:** flags WITHOUT a bot.yml passthrough (`HOT10_CARD`, `METRICS`) won't take effect from a bare `gh variable set` — they need the env line added to `.github/workflows/bot.yml` first (a tiny PR).

## Open decisions — all AWAITING ANDREW (do not act without his go)

1. **Remaining dark flags** — supply cap raise, reganom, concurrency, engagement window. Each is his cost/review/taste call; recommendation stands that the *supply* levers are the highest-leverage for fewer no-draft days.
2. **PR #266 — the SOURCE-REDUNDANCY lane** — pursue (fire the Codex kickoff) or close. Parked pending his decision.
3. **reganom flip** — one variable; the readiness runbook shipped in S-28.
4. **`OPENAQ_API_KEY` secret** — unlocks the S-25 AQ corroboration that shipped dark.
5. **S-24 (record margin-percentile)** — BLOCKED on missing data, not a bug. Needs a margin-history pipeline if ever wanted.

## The two execution lanes — document map

- **THIRTY-LOOP (COMPLETE):** [/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-06-11-thirty-loop.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/2026-06-11-thirty-loop.md) + `-PROGRESS.md` (all rows DONE; S-24 blocked) + `-CODEX-KICKOFF.md`.
- **SOURCE-REDUNDANCY (PLANNED, parked in [#266](https://github.com/andrewzp/theheat/pull/266)):** `docs/superpowers/plans/2026-06-13-source-redundancy-lane.md` + `-PROGRESS.md` (these files live on the `docs/second-witness-lane` branch, NOT on `main`, until #266 is merged). 10 steps R-00..R-09; the §L block is its Codex kickoff. Born as a MERGE of two same-night drafts; **decision rule (Andrew): an independent backup feed MAY create a draft during a host outage**, tagged via the existing `evidence_grade` mechanism (`observed_alt_host`/`model_fallback`, the air-quality CAMS pattern) so it never claims an observation it didn't make, still manual-queue gated. Key verified feeds: NOAA HMS (firms), Open-Meteo precip+ensemble (gpm), ReliefWeb (gdacs), Open-Meteo Flood (river_gauges), FIRMS product chain, CRW ERDDAP (coral). R-09 sea-ice needs a parser dep (`netCDF4`/`pyhdf`) = Andrew's call. Codex-reviewed (5 P0s fixed; ocean_sst + global-FDH cut as out-of-scope).
- **The audit (the why):** [/Users/andrewpuschel/Documents/Claude/theheat/docs/audits/2026-06-10-thirty-improvements-and-outage-plan.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/audits/2026-06-10-thirty-improvements-and-outage-plan.md).

## Running the redundancy lane IF Andrew greenlights it

Merge #266 first (`gh pr merge 266 --squash --delete-branch`), then from a fresh Codex session, paste the §L kickoff from the lane doc (token-free execution off Anthropic). It inherits THIRTY-LOOP's rails. Until he says go, leave #266 open and untouched.

## First 5 minutes — orient

```bash
cd /Users/andrewpuschel/Documents/Claude/theheat
git fetch && git checkout main && git pull && cat VERSION          # expect 0.9.63.0+
git log --oneline -6 origin/main
gh pr list --state open --json number,title -q '.[] | "OPEN #\(.number) \(.title)"'   # #207 + maybe #266; leave both unless Andrew directs
gh issue list --state open --label source-health-sentinel --json number,title,labels -q '.[] | "#\(.number) [\(.labels|map(.name)|join(","))] \(.title)"'
gh variable list --repo andrewzp/theheat | grep THEHEAT          # WRITER_SAMPLES=2, CRITIC_REVISE=1, GPM_SOURCE=datapool
gh api repos/andrewzp/theheat/actions/variables/THEHEAT_REGANOM_ENABLED --jq '.value' 2>/dev/null || echo DORMANT
# pipeline state
gh gist view 06c02c97ffc0d11458687f1ed998d9e5 -f state.json > /tmp/st.json && jq '{state_kb:(.|tostring|length/1024|floor), drafts_pending:(.drafts//[]|map(select(.status=="pending"))|length)}' /tmp/st.json
# sanity (suite ~4s)
source .venv/bin/activate
python -m mypy src/ 2>&1 | tail -1
python -m pytest tests/ -q -m "not voice_replay" --tb=no 2>&1 | tail -1
(cd dashboard && node --test 2>&1 | grep -E '^# (pass|fail)')
```

## Standing constraints (read every session)

- **Auto-fix failures without asking.** Surface only taste calls, direction reversals, scope expansions, destructive ops.
- **No push to `main`.** All via PR (incl. docs). Claude merges PRs when CI-green + approved: `gh pr checks <N> --watch` → `gh pr merge <N> --squash --delete-branch` → `git checkout main && git pull`. theheat requires the `test` check; `--auto` is disabled.
- **Dashboard deploy is manual** (`vercel --prod` from `dashboard/`); verify with `curl -s -o /dev/null -w "%{http_code}" https://dashboard-phi-beryl-65.vercel.app` → 401.
- **Never** set repo variables/secrets, flip reganom, disable workflows, or edit the gist by hand without Andrew's explicit go. **Never touch PR #207.**
- **Keep the Python sentinel (`scripts/source_health_sentinel.py`) and dashboard JS classifier (`dashboard/lib/source-health.js`) in sync** — same PR, both test suites.
- **New `DEFAULT_STATE` keys REQUIRE a `MERGE_SPEC` entry** (structural test enforces); regenerate the golden fixture (`python scripts/gen_merge_golden.py`) only on intentional semantics changes + review the diff.
- **`cd` into the repo on EVERY Bash command** (the Bash tool persists cwd — a `cd dashboard` earlier WILL leak into later commands; this bit me this session reading a nonexistent `.github/workflows/bot.yml`). Activate the venv before any python.
- **iCloud drops `"<name> 2.<ext>"` duplicates** into the tree; they're gitignored, delete on sight after `diff -q`.
- **Check https://status.claude.com FIRST** if a Claude surface looks broken.
- **Keep an outside voice (Codex) in the loop for high-stakes plans** — it caught real P0s in both the THIRTY-LOOP plan and the redundancy plan this session.

---

## Session resume prompt (paste verbatim into a fresh Claude Code session)

> Resume @theheat. Canonical context: `/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-06-13.md` (this file). Read it in full, then run the **First 5 minutes** block before any new work. Production state (gist `state.json`, live dashboard, GitHub PRs/issues/variables, claude.ai Routines UI) is the source of truth and outranks the handoff.
>
> Standing directive (user memory `feedback_auto_fix_failures.md`): when something is failing, diagnose and fix it without asking; surface only taste calls, direction reversals, scope expansions beyond the failure, and destructive operations.
>
> State of play (2026-06-13):
> - **THIRTY-LOOP COMPLETE.** Codex shipped all 30 audit fixes (S-01..S-35, PRs #222–#265, main 0.9.62.0/0.9.63.0); only S-24 is BLOCKED (no margin-history in the thresholds DB). "Goal seek project" = these 30 fixes — there is no separate project. Dashboard deployed.
> - **Two supply flags are LIVE:** `THEHEAT_WRITER_SAMPLES=2` + `THEHEAT_CRITIC_REVISE_ENABLED=1` (best-of-2 + one rewrite; manual queue still gates posting).
> - **THE ACTIVE NEXT JOB: execute the SOURCE-REDUNDANCY lane (GREENLIT by Andrew 2026-06-13).** Plan: `docs/superpowers/plans/2026-06-13-source-redundancy-lane.md` (R-00..R-09); diligent kickoff: `docs/superpowers/plans/2026-06-13-source-redundancy-CODEX-KICKOFF.md`; state: `…-source-redundancy-PROGRESS.md`. Independent backup feeds (NOAA HMS, Open-Meteo precip+ensemble, ReliefWeb, Open-Meteo Flood) that MAY draft during a host outage — tagged `evidence_grade`-honest (`observed_alt_host`/`model_fallback`), manual-queue gated. Run it Codex-token-free per the kickoff: one PR per step, verify-live-feed → TDD → implement-to-spec → gates-with-pasted-evidence → self-review on `[CODEX]` steps → honesty checklist. R-00 is the mandatory foundation; R-02..R-05 are the point. R-09 (sea ice) STOPs for Andrew on a parser dep. Don't re-plan it — it's Codex-reviewed and ready.
> - **Root cause of no-draft-days is mostly editorial SUPPLY, not outages** (funnel 924k→26k→6; triage caps killed 135 vs 65 by all gates) — redundancy fixes only the outage subset, so keep the supply flags in view too.
> - Workflows ACTIVE: theheat-bot, refresh-thresholds, voice-regression, source-health-sentinel — do NOT disable. reganom DORMANT. posting manual_only. source-health clean.
> - **Still dark, all Andrew's call:** `MAX_DRAFTS_PER_CYCLE`, `REGANOM`, `CONCURRENT_SOURCES`, `ENGAGEMENT_WINDOW` (one-liner flips); `HOT10_CARD`, `METRICS` (need a bot.yml passthrough first); `OPENAQ_API_KEY` (needs a secret).
>
> Constraints: no push to main (all via PR, incl. docs); Claude merges PRs (theheat needs the `test` check, `--auto` disabled → `gh pr checks <N> --watch` then squash-merge `--delete-branch`; after merge `git checkout main && git pull`); dashboard deploy is manual (`vercel --prod`, verify 401); never set repo variables/secrets, flip reganom, or disable workflows without Andrew's go; never touch PR #207; keep the Python sentinel + dashboard JS classifier in sync; new `DEFAULT_STATE` keys require a `MERGE_SPEC` entry; `cd` into the repo on every Bash command (cwd persists) + activate the venv; absolute clickable file links; check status.claude.com first if a Claude surface looks broken; keep an outside voice (Codex) on high-stakes plans.
>
> **Your job this session: kick off and shepherd the SOURCE-REDUNDANCY lane.** The execution itself runs token-free on Codex — launch it with the command in `docs/superpowers/plans/2026-06-13-source-redundancy-CODEX-KICKOFF.md` (Part 1), then monitor: watch the PROGRESS table fill, the `loop/r<NN>-…` PRs merge green, and the touched source's health row stay clean on the next scheduled run. Triage anything the lane marks `BLOCKED`/`AWAITING-ANDREW` (notably R-09's parser-dependency STOP). Do NOT re-audit, re-plan, or re-run completed work; do NOT execute R-09's dependency or flip any flag without Andrew's explicit go.
