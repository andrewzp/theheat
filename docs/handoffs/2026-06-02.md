# Resume @theheat — Next Session Prompt (2026-06-02)

This brief **supersedes** [/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-06-01.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-06-01.md). The 06-01 handoff was stale on two counts that this session corrected: it claimed the daily-plan routine was "writing reliably" (it has been **dead since 05-26**) and listed the C5 source-health panel as "not built" (it was built ~05-14 and this session **fixed + deployed** it).

## TL;DR

- **Main at 0.9.11.0.** Three releases shipped this session: 0.9.9.0 (#167), 0.9.10.0 (#168), 0.9.11.0 (#169). Open PRs: 0.
- **Source-health dashboard panel fixed + deployed** (0.9.9.0) — recency-aware health classification + corrected success-rate fraction. Live on Vercel (`dashboard-phi-beryl-65.vercel.app`).
- **Source-fetch reliability sweep done** — every *fixable-on-our-side* fetch failure is addressed. 0.9.10.0 killed gpm_imerg's dominant 404 (date walk-back); 0.9.11.0 killed the IPv6-unreachable class (global IPv4 force) + added default UA + routed ice_mass/river_gauges retries. Remaining yellow is upstream (NASA 503/502, intermittent gov 403s that are rate-limits, not UA-blocks — tested).
- **⚠️ The daily-plan grading routine is DOWN since 2026-05-26.** It fires daily but produces zero artifacts (CCR-env failure). **This is the #1 open thread** — it's the A-rate instrument that gates the posting flip. Fix is operator-only (re-auth or rebuild). Not urgent (0 drafts to grade) but blocks the posting decision.
- **Andrew pushed a tweet live 2026-06-02** (manual). Posting still `manual_only` for all categories.
- **Tests:** mypy clean (92 files), pytest 1372 passing (excl voice_replay), dashboard 45/45.

## Session resume prompt (paste verbatim into a new Claude Code session)

```
Resume @theheat. Canonical context: /Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-06-02.md (this file).

Pre-session check: read the handoff in full, then run the First 5 Minutes block before any new work. Production state is the source of truth — the gist state.json and live dashboard outrank this snapshot.

Standing directive (persisted in user memory feedback_auto_fix_failures.md):
"When something is failing, diagnose and fix it without asking. Surface only taste calls, direction reversals, scope expansions beyond the failure, and destructive operations. Andrew wants execution, not options-menus."

State of play after 2026-06-02:
- Main at 0.9.11.0. Bot workflows (theheat-bot, voice-regression) ACTIVE; refresh-thresholds active. Do NOT disable without Andrew's go.
- Three releases shipped 2026-06-02:
  - 0.9.9.0 (#167) — dashboard source-health: recency-aware classification (last 5 runs, not 7-day cumulative) + success-rate fraction uses active (non-skip) denominator. Deployed to Vercel.
  - 0.9.10.0 (#168) — gpm_imerg date walk-back: _resolve_available_date probes back day-by-day to the latest published Late file instead of blindly requesting yesterday (which 404s before NASA publishes). Kills the dominant gpm failure.
  - 0.9.11.0 (#169) — shared HTTP hardening: force_ipv4() (urllib3 HAS_IPV6=False at import) kills the [Errno 101] Network-unreachable class on the IPv6-broken runner; fetch_with_retry injects a default UA; ice_mass PO.DAAC fetch + river_gauges USGS call route through fetch_with_retry for retry+backoff.
- Andrew pushed a tweet live 2026-06-02 (manual). Posting still manual_only.

The #1 open thread: the daily-plan grading routine is DOWN.
- It is a claude.ai RemoteTrigger (NOT a GitHub Actions workflow): id trig_016PGeHZgEYWmeQhx1xGmYg6, cron "0 15 * * *", CCR env env_0144W6SAcDXhG6BD5ZS249Bp.
- It fires daily (last_fired_at advances) but has produced ZERO artifacts since 2026-05-26: the ROUTINE_BEACON repo variable, docs/QUALITY_TREND.md, and the rolling daily-plan-current PR are all frozen at 05-26.
- Git logic is sound (local repro of Step 0 / Step 8 passes). The prompt was last edited 2026-05-26 20:58 UTC — coincident with the failure start. Leading hypotheses: a Step 0 bug, a dead/expired CCR env token, or CCR routine-infra flakiness. Cannot be confirmed from a CC session — the run log lives only in the claude.ai Routines UI (panel at https://claude.ai/code/routines; the per-ID deep link does NOT resolve).
- Inspect/operate via the RemoteTrigger tool: action get/list/run/update. A manual `run` produced no artifacts in 8 min.
- Fix paths (operator-gated): (a) Andrew opens the run log in the Routines UI and pastes the error → apply a precise prompt fix via RemoteTrigger update; (b) rebuild the routine via RemoteTrigger create with a hardened Step 0 + fresh env (clears env-token cause), preserving the grading prompt + constraints, then disable the old trigger. See operator memory reference_theheat_daily_plan_routine.md.

Don't pile new architectural work on top until the routine is restored and the source-reliability sweep's impact has shown across a cron cycle or two. The remaining big-ticket items (the @extremetemps coverage gap; C5 per-source auth/credential panel; theheat.ai landing page) are candidates, not commitments — discuss before building.
```

## First 5 minutes — orient

```bash
cd /Users/andrewpuschel/Documents/Claude/theheat

# 1. Confirm main — expect 0.9.9.0–0.9.11.0 at top
git fetch && git log --oneline -6 main && cat VERSION   # expect 0.9.11.0

# 2. Workflows MUST be active: theheat-bot, voice-regression, refresh-thresholds
gh workflow list --all

# 3. Recent bot runs (trend) + open PRs (expect 0)
gh run list --workflow=theheat-bot --limit 8 --json conclusion,event,createdAt --jq '.[] | "\(.createdAt)  \(.conclusion // "-")  \(.event)"'
gh pr list --state open --limit 5

# 4. Pipeline state (the key delta)
gh gist view 06c02c97ffc0d11458687f1ed998d9e5 -f state.json > /tmp/state.json && jq '{
  drafts_pending: (.drafts // [] | map(select(.status=="pending")) | length),
  latest_run: ((.run_history // []) | sort_by(.started_at) | reverse | .[0] | {status, drafted_count, failure_count, ended_at}),
  new_critic_kills_today: (.suppressions // [] | map(select(.ts > "2026-06-02T00:00:00Z" and .stage=="critic")) | length),
  recent_critic_kill_reasons: (.suppressions // [] | map(select(.stage=="critic")) | sort_by(.ts) | reverse | .[0:5] | map(.reasons // [] | .[0]))
}' /tmp/state.json

# 5. SOURCE RELIABILITY — did 0.9.10.0 + 0.9.11.0 land? Watch gpm_imerg climb, firms→100%, ice_mass 502s recover
jq -r '.source_health | to_entries | sort_by(.value.success // 0) | .[] | "\(.key)\tS=\(.value.success // 0) F=\(.value.failed // 0)\t\((.value.last_error // "-")[0:60])"' /tmp/state.json
#  - gpm_imerg: success counter should climb (404 walk-back), ConnectionError/Network-unreachable should vanish (IPv4)
#  - firms: should return to ~100% (IPv4)
#  - ice_mass: 502s now retry; still monthly + NASA-side so expect occasional yellow
#  - 403s (coral_dhw/jtwc/river_gauges/copernicus_ems): upstream rate-limits, NOT fixable — leave alone

# 6. THE ROUTINE CANARY — is it still dead?
gh api repos/andrewzp/theheat/actions/variables/ROUTINE_BEACON --jq '.value | fromjson'
#  - If routine_last_run_at is still 2026-05-26 → STILL DOWN. If it shows a recent date → Andrew re-armed it; verify QUALITY_TREND.md got a fresh row.
git log -1 --format='%ci  %s' -- docs/QUALITY_TREND.md   # last update; frozen at 2026-05-26 = routine still down

# 7. Sanity (tests + types)
source .venv/bin/activate
python -m mypy src/ 2>&1 | tail -2          # expect: Success: no issues found in 92 source files
python -m pytest tests/ -q -m "not voice_replay" --tb=no 2>&1 | tail -2   # expect: 1372 passed
(cd dashboard && npm test 2>&1 | tail -4)   # expect: tests 45, pass 45
```

**Snapshot at handoff time (2026-06-02 ~21:00 UTC):**

- VERSION `0.9.11.0`; mypy clean (92 files); pytest 1372 passing; dashboard 45/45.
- `theheat-bot`, `voice-regression`, `refresh-thresholds`: **active**. Open PRs: 0.
- Pending drafts: 0 (editorial-supply bottleneck; the funnel is healthy, just producing nothing under the editorial bar yet).
- Source health (pre-sweep-impact, from the panel Andrew shared): gpm_imerg 16% (will climb), ice_mass 33% (monthly/NASA), firms/ocean_sst/coral_dhw/jtwc/copernicus_ems/river_gauges 95–97% (transient). The 0.9.10.0/0.9.11.0 fixes land on the *next* crons — verify the trend over 24–48h, don't conclude from a single run.
- Daily-plan routine: **DOWN** (beacon frozen at 2026-05-26T15:01:51Z; last_fired_at advancing daily).
- Anthropic note: the claude.ai Code web app intermittently showed "Claude will return soon" on 2026-06-02 (status.claude.com read operational — status pages can lag). If a Claude surface looks broken next session, **check https://status.claude.com FIRST** (operator memory feedback_check_claude_status.md).

## What landed this session (2026-06-02)

| PR | Release | What |
|---|---|---|
| [#167](https://github.com/andrewzp/theheat/pull/167) | 0.9.9.0 | **Dashboard source-health: recency + fraction.** `classifyHealth` classifies on the last `RECENT_WINDOW` (5) runs, not the 7-day cumulative counters — recovering sources fall to `degraded` not `unhealthy`; freshly-degrading ones flip to `unhealthy` early. Success-rate fraction uses `active` (= success+failed+degraded+partial, excludes skips) so the `(N/M)` matches the `%` (no more `33% (1/10)`). Files: `dashboard/lib/source-health.js`, `dashboard/app/page.js`. Deployed via `vercel --prod` (manual cadence — prod was ~2.5 weeks behind main; this also brought other merged dashboard changes current). Dashboard 41 → 45 tests. |
| [#168](https://github.com/andrewzp/theheat/pull/168) | 0.9.10.0 | **gpm_imerg date walk-back.** The dominant gpm failure was HTTP 404 (Late product lags 1-2 days; the code requested yesterday and treated 404 as non-retryable → silent total failure). New `_resolve_available_date` in `src/data/gpm_imerg.py` probes back day-by-day to the latest published file (200 → use; 404 → step back; transient → stop). Default-path only; explicit `target_date` bypasses. `GPM_IMERG_MAX_LOOKBACK_DAYS` (default 5). +3 tests. |
| [#169](https://github.com/andrewzp/theheat/pull/169) | 0.9.11.0 | **Shared HTTP hardening.** `force_ipv4()` in `src/data/_http.py` flips `urllib3 HAS_IPV6=False` at import (runner has broken IPv6; EOSDIS hosts publish AAAA → `[Errno 101] Network unreachable`). `fetch_with_retry` injects a default User-Agent. `ice_mass.py` (PO.DAAC data fetch) + `river_gauges.py` (USGS call) route through `fetch_with_retry` for retry+backoff. +4 tests. |

**Operator actions / facts this session:**

- Andrew pushed a tweet live 2026-06-02 (manual posting path works).
- Dashboard deployed to Vercel production (`vercel --prod` from `dashboard/`; project `prj_RYPr6fRdFQkvOREmauO7zwU21My3`, team `team_K0UgDwwprGyhzJbEGsh7hAOT`, alias `dashboard-phi-beryl-65.vercel.app`). **Vercel deploys are MANUAL here, not auto-on-main** — a bot change is live next cron, but a dashboard change needs `vercel --prod`.
- 403-on-gov-sources hypothesis (UA block) was **empirically tested and refuted** — coralreefwatch.noaa.gov, api.water.noaa.gov, metoc.navy.mil, rapidmapping.emergency.copernicus.eu all return 200 with default/python-requests/browser UAs. The 403s are intermittent rate-limits. Don't add UA/IPv4 changes for them.

## The #1 open thread: restore the daily-plan grading routine

It is a **claude.ai RemoteTrigger**, NOT a GitHub Actions workflow (won't appear in `gh workflow list`):

- ID `trig_016PGeHZgEYWmeQhx1xGmYg6`, cron `0 15 * * *`, CCR env `env_0144W6SAcDXhG6BD5ZS249Bp`. Inspect/operate via the `RemoteTrigger` tool (`get`/`list`/`run`/`update`).
- **Symptom:** fires daily (`last_fired_at` advances) but ZERO artifacts since 2026-05-26 — `ROUTINE_BEACON`, `docs/QUALITY_TREND.md`, `docs/DRAFT_CORPUS.md`, and the rolling `daily-plan-current` PR all frozen at 05-26.
- **Diagnosis so far:** prompt git logic is sound (local repro of Step 0 + Step 8 passes); the prompt's `updated_at` is 2026-05-26 20:58 UTC, right when failures began. Cause unconfirmed (Step 0 bug / dead env token / CCR infra) — the run log is only in the claude.ai Routines UI (panel at https://claude.ai/code/routines; the per-trigger-ID deep link does NOT resolve), which a CC session cannot read. A manual `run` produced nothing in 8 min.
- **Fix paths (operator-gated):**
  1. Andrew opens the latest run's log in the Routines UI, pastes the one-line error → apply a precise fix via `RemoteTrigger update`.
  2. Rebuild via `RemoteTrigger create` with a hardened Step 0 + fresh env (clears the env-token cause), preserving the grading prompt + all `DO NOT` constraints, then disable the old trigger. This is more disruptive (new trigger ID, loses beacon/PR continuity) — get Andrew's go.
- **Why it matters:** it's the A-rate grading instrument. The pipeline + reliability are no longer the blockers to the posting flip — the missing grading data is. Restore it before the A-rate observation window can produce anything.
- Full operational detail in operator memory `reference_theheat_daily_plan_routine.md`.

## What's NOT done (next-session candidates — discuss before building)

1. **Restore the daily-plan routine** (above). Highest priority once Andrew can access the Routines UI or greenlights a rebuild.
2. **Watch the reliability sweep land** (no code) — confirm over 24–48h that gpm_imerg climbs, firms → ~100%, ice_mass 502s self-heal. If gpm is still bad after a day, the residual is NASA 503 overload (upstream, not fixable).
3. **The @extremetemps coverage gap** (unchanged from 06-01). Bot is a station-record detector; @extremetemps is an astounding-temperature curator. Two additive PRs: (A) `absolute_extreme` signal type in `open_meteo.py` (latitude-banded thresholds); (B) `src/orchestrator/sources/reanalysis_anomaly.py` (GFS/ERA5 country-scale anomalies). ~80% of the gap. Brainstorm + design before code.
4. **C5 dashboard — per-source auth/credential panel.** The health panel exists + is now accurate (0.9.9.0); the unbuilt part is per-source auth/credential visibility (which token, is it valid). Higher value now that source failures are surfaced honestly.
5. **theheat.ai landing page** — domain parked on GoDaddy.
6. **Posting-mode flip** — gated on A-rate, gated on the routine being alive.

## Operator constraints (read every session)

Unchanged from 06-01 plus this session's additions:

- **Auto-fix failures without asking.** Surface only taste calls, direction reversals, scope expansions, destructive ops.
- **Check https://status.claude.com FIRST** when a Claude surface looks broken (blank page, link "does nothing", "Claude will return soon") — don't assume browser/URL/config. (memory `feedback_check_claude_status.md`)
- **Bot vs dashboard deploy:** the Python bot goes live on the next cron after merge-to-main (no deploy). The **dashboard is a manual Vercel deploy** (`vercel --prod` from `dashboard/`) — merging is NOT enough to make a dashboard change live.
- **Don't thrash external source flakiness.** NASA 503/502 + gov 403s are upstream and self-recover; fix only what's fixable on our side (done this session). Test hypotheses (UA, IPv4) against the real endpoint before changing code.
- **Claude merges PRs, not Andrew.** CI-green + approved → `gh pr merge <N> --squash --delete-branch`. theheat requires the `test` check; `--auto` disabled — `gh pr checks <N> --watch` then merge.
- **No push to main.** All via PR. Plan before non-trivial code. Absolute paths in file links.

## Don't touch without re-reading (additions from 0.9.9.0–0.9.11.0)

- **`dashboard/lib/source-health.js`** — `classifyHealth` recency logic reads `recent_active`/`recent_successes` (only present on the durable `source_health` path; the `run_history` fallback path has no recent window and uses cumulative — intentional). `addDerivedFields` exposes `active`; the table fraction in `dashboard/app/page.js` renders `(successes/active …, N skipped)`. Locked by `tests/source-health.test.js`.
- **`src/data/gpm_imerg.py`** — `_resolve_available_date` walk-back runs only when `target_date is None`. It uses a `.ascii` probe at a fixed reference point; 404→back, 200→use, transient→stop (safe degradation). Don't make it walk back on non-404.
- **`src/data/_http.py`** — `force_ipv4()` is called at import; the bot imports all sources at startup so it runs before any fetch. `fetch_with_retry` injects `_DEFAULT_USER_AGENT` only when the caller sets none (case-insensitive check) — don't override caller UAs.
- Plus everything from the 06-01 handoff's "Don't touch" section (gpm retry layer, triage diversity gate/TTL, fact_check unknown-kind skip, critic period-of-record bullet) — all unchanged.
