# Handoff — 2026-07-03

**`main` @ `4dc446c`** (+ follow-ups #356 test-fix, #357 dashboard-sort landing after),
version **0.9.81.0** (changes accumulate under CHANGELOG `[Unreleased]`). Supersedes
[docs/handoffs/2026-06-28.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-06-28.md).

> **★ North star — read first:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md)
> — what @theheat is trying to achieve (editorially excellent tweets; interesting sourced
> anecdotes like a good news story; global coverage) and the failure we are organizing
> around: **@theheat missed the European heat wave that killed 1,200+ people ENTIRELY.**
> Bet A exists to close that gap.

## TL;DR

Picked up the 2026-06-28 P1 (fix the "boring" reganom tweets) — shipped the voice
upgrade — but the session turned into a **product-level reckoning**. Two deadly events
(the European heatwave, 1,300+ dead; the Western US wildfire outbreak, 3 firefighters
killed) were missing from @theheat, and digging in exposed **why the bot reads silent on
the stories that matter**: it was **out of Anthropic API credits** (writer down),
**good drafts were dying in an unwatched manual queue** (autoship allowlist too narrow),
and — the root problem — **selection ranks by raw sensor magnitude and is blind to
newsworthiness / human stakes** (it *posted* a remote 1,468 MW Congo Basin fire while
*suppressing* the deadly Colorado fire at 62<64).

Andrew's verdict after a lot of spend: *"we've added a lot of money and we still aren't
happy with the results."* We reframed the path as two bets and **Andrew chose Bet A —
make autonomous selection smart** (newsworthiness + sourced human impact). We are
**mid-brainstorm on Bet A** (3 design decisions locked); that is the next session's main
work.

**Credits are restored** (Andrew topped up) — the writer is confirmed drafting again
(voice-regression green, full-length bot runs).

## What shipped (both merged to `main`)

### #349 — reganom writer-voice upgrade (merged `0055cb7`)
The reganom France draft was honest but read like a stats bulletin ("undersells what has
been happening"). Rewrote the **"Regional anomaly bundles"** section of
[src/two_bot/prompts/writer_prompt.py](/Users/andrewpuschel/Documents/Claude/theheat/src/two_bot/prompts/writer_prompt.py)
around four moves: **weave** the sampled-cities attribution (don't open on it), **round**
the headline + drop the noise °F, **σ as awe** not a beat, **name the stakes**.
- **Rounding, done safely:** [src/two_bot/intern/temperature.py](/Users/andrewpuschel/Documents/Claude/theheat/src/two_bot/intern/temperature.py)
  now publishes `headline_metric.value_rounded_c = round(mean_anomaly_c)` so the writer
  cites a bundle value **verbatim** (e.g. "~12°C") instead of rounding 11.53 itself and
  tripping the strict BUNDLE_FACT precision-kill (same pattern as `zscore_1dp`).
- **Fact-check** [src/two_bot/prompts/fact_check_prompt.py](/Users/andrewpuschel/Documents/Claude/theheat/src/two_bot/prompts/fact_check_prompt.py):
  rule **g** blesses `value_rounded_c` (whole-degree must equal it exactly; finer decimal
  within ±0.1°C of raw); **new rule j3** — reganom is **daily-max only**, so
  overnight-low / "no nighttime relief" / health / infrastructure claims are UNVERIFIABLE.
- **New offline harness** [scripts/reganom_writer_dryrun.py](/Users/andrewpuschel/Documents/Claude/theheat/scripts/reganom_writer_dryrun.py)
  + **dispatch workflow** [.github/workflows/reganom-dryrun.yml](/Users/andrewpuschel/Documents/Claude/theheat/.github/workflows/reganom-dryrun.yml)
  (workflow_dispatch): runs the real writer → safety → §F → fact-check → critic in
  Actions (where the API keys live) and prints every candidate + verdict to the log. This
  is how we iterate reganom voice without a prod gist reset. `gh workflow run
  reganom-dryrun.yml --ref <branch>` runs any branch's prompt.
- codex-xhigh **3 rounds → clean APPROVE** (caught: a "no nighttime reset" unsupported
  claim → rule j3; a residual "~11°C" example contradicting value_rounded_c=12; a rule-g
  ±0.5 escape hatch). ruff + mypy + full pytest green.

### #352 — autoship allowlist → verifiable record types (merged `4dc446c`)
The autoship-on-critic-PASS allowlist was only `{hot10, co2_milestone, ch4_milestone}` —
so all-time highs, monthly records, marine-heatwave records were **all manual_only** and
died unposted in an unwatched queue. Principle: **auto-ship verifiable RECORDS; keep
anomalies and human-impact events manual.** Added `all_time_high/low`, `monthly_high/low`,
`marine_heatwave` to `AUTOSHIP_ALLOWLIST` ([src/editorial/approval.py](/Users/andrewpuschel/Documents/Claude/theheat/src/editorial/approval.py)).
Still manual: calendar records (volume), `regional_anomaly` + anomalies (interpretive),
`country_high/low` (kept a human polish window), all human-impact types. Kill-switch
unchanged: `THEHEAT_AUTOSHIP_ON_CRITIC_PASS=0`. codex-xhigh APPROVE (verified the type
strings are the real persisted `draft["type"]`). +8 tests; 2115 pytest pass.

## ⚠️ Operational discoveries (the real story of the session)

1. **Anthropic credits were exhausted → writer down.** voice-regression went red (every
   fixture 400'd on `"credit balance is too low"` in 39s). The bot kept running "success"
   (graceful BudgetExhaustedError) but drafted nothing new. **RESOLVED — Andrew topped up
   credits; writer confirmed back** (voice-regression green 2026-07-03 11:36). *Lesson for
   the handoff: theheat's writer is the metered Anthropic API, separate from Andrew's
   Claude Code Max plan; a low balance silently stops drafting while runs stay green.*
2. **Good drafts stuck in the manual queue.** The dashboard had **10 drafts** awaiting
   review, none posting — including a strong **Prudhoe Bay 101°F all-time high** (8h old)
   and a marine-heatwave record — because they were manual_only. #352 fixes this **going
   forward**, but those 10 were drafted **pre-#352**, so they will **not** retroactively
   auto-ship. **ACTION FOR ANDREW: on the dashboard, Approve+Post the good ones and Reject
   the old France `REGIONAL_ANOMALY` bulletin** (it has the "even brief nighttime relief
   disappears" line — an unsupported daily-max claim the new prompt + rule j3 forbid).
3. **Selection is magnitude-ranked, newsworthiness-blind (the root problem → Bet A).**
   Smoking gun: on 2026-06-29 the bot **posted** *"1,468 MW … Congo Basin, DR Congo"* while
   **suppressing** the Colorado fires (`fire 62 < 64`). It ranks big numbers, not big
   stories, and has no path to the human toll (deaths, firefighter fatalities).

## reganom stakes-attribution voice fix — ⭐ READY, needs codex + PR (branch `fix/reganom-stakes-attribution`, pushed `387cb1a`, NOT merged)

The **live dispatch** of #349's prompt (run on `main`) surfaced that the merged exemplar
**personifies France** — *"France was effectively living a hotter country's summer"* /
*"France's afternoons…"* — which the honesty fact-check (rule j) **kills as a bare-region
generalization** of the sampled-city point index. So **`main`'s reganom exemplar currently
teaches a phrasing that fails fact-check.** The `fix/reganom-stakes-attribution` branch
fixes it: move 4 + the exemplar now keep the **sampled cities** as the grammatical subject
of the stakes clause (never the personified country), and state the duration ONCE ("for a
week" alongside "eight days" reads as a 7-vs-8 mismatch). **Live-verified 3/3 candidates
shipping** (writer → safety → §F → fact-check → critic) via `gh workflow run
reganom-dryrun.yml --ref fix/reganom-stakes-attribution`. The three passing candidates
(only the stakes clause varies):
- **①** "…a sustained 2.8σ anomaly, the margin where 'above average' stops describing it.
  **Those cities' afternoons were living a different, hotter summer.** It eased June 28." (267)
- **②** "…**Those cities' afternoons were effectively living a hotter country's summer.**
  It eased June 28." (278, closest to Andrew's original target)
- **③** "…**Those cities' afternoons were living a hotter climate's summer.** It eased June 28." (266)

**NEXT-SESSION ACTION:** this branch needs (a) a **codex-xhigh pass** (prompt change to an
editorial honesty gate), (b) **a PR + merge** — it's independent of which France draft
Andrew posts (Andrew's ①/②/③ preference is only a tiny exemplar tweak, not a blocker), and
it repairs a real defect on `main`. Then the France reganom draft can be regenerated (it's
manual_only) and posted.

## ⭐ NEXT SESSION — the main work: Bet A (newsworthiness + sourced human-impact)

Andrew chose **Bet A** ("make autonomous selection smart") over Bet B ("assistant /
human-curated"). We are **mid-`superpowers:brainstorming`**, paused after Q3, with 3
decisions locked. **Full state — read this first:**
[docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md).

**Locked:** (1) **Hybrid source** — authoritative feeds (NIFC/WHO/NWS) + grounded LLM
search. (2) **v1 = re-rank + enrich** what the bot already detects (boost by newsworthiness
so Colorado beats Congo; attach sourced human-impact so tweets carry "3 firefighters
killed") — the new-coverage-trigger is a v2 fast-follow. (3) **Boost = rescue, capped** — a
sourced newsworthiness match can rescue a below-threshold signal (Colorado 62<64), bounded,
source-required, hard floor. **THE IRON CONSTRAINT: every figure from real cited retrieval,
never the model's imagination.**

**Resume the brainstorm from Q4** (autonomy for impact tweets — recommend manual_only) →
**Q5** (MVP feeds — recommend NIFC fire + grounded search for heat mortality) → propose
2–3 approaches → present design sections → write/commit the completed spec → user reviews →
invoke `superpowers:writing-plans`. **Before designing, READ the prior art** flagged in the
design doc: `docs/superpowers/specs/2026-06-25-coverage-monitor-design.md` (a sibling
coverage-gap design, codex-reviewed, awaiting Andrew's review — reconcile, don't reinvent),
IDEAS.md items #9/#10, and the existing GDACS sourced-impact lane.

## Held / follow-ups (carried)

- **Hand-sourced manual drafts (ready to post if Andrew wants):** heat deaths — *"Europe's
  heatwave is linked to more than 1,300 excess deaths since June 21, the WHO says — about
  1,000 in France, most over 65. The six French cities @theheat tracks had just run eight
  straight days ~12°C above their 1991–2020 normal. The heat is now shifting east."*;
  Western fires — *"Three firefighters were killed Saturday on the Knowles and Gore fires
  at the Colorado–Utah border, as a hot, dry, windy weekend lit up the West: 100+ new fires
  in 72 hours, dozens of large blazes burning, the US now at Preparedness Level 4 — its
  second-highest."* (Every figure sourced: WHO/France24; NIFC/CPR/Al Jazeera. The bot
  can't produce these yet — that's Bet A.)
- **[#346](https://github.com/andrewzp/theheat/pull/346)** dup-city fix — OPEN, HELD:
  needs same-place alias dedup (Dubai/Amsterdam/Hong Kong/Kinshasa/Brazzaville) in
  [data/cities.csv](/Users/andrewpuschel/Documents/Claude/theheat/data/cities.csv), then
  re-run codex on the correct branch checkout.
- **UK & Ireland reganom draft** — killed for length, onset-suppressed; needs a
  writer-length fix + guard clear.
- **Coverage-monitor design** ([docs/superpowers/specs/2026-06-25-coverage-monitor-design.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/specs/2026-06-25-coverage-monitor-design.md))
  — Proposed v2, awaiting Andrew's review; now folds into the Bet A conversation.
- **Carried, unchanged:** `SELFHEAL_PAT` ([#309](https://github.com/andrewzp/theheat/issues/309));
  claim/warrant ([#324](https://github.com/andrewzp/theheat/pull/324), docs-only);
  "recently posted" dashboard feed.

## Rollout watch

- **#352 autoship expansion is live.** Now the writer's back, the next runs that draft an
  all-time high / monthly record / marine-heatwave record with a critic PASS should
  **auto-post on the guarded delayed window** (90–120 min) instead of queuing. Watch the
  dashboard "auto" count go nonzero. Kill-switch: `gh variable set
  THEHEAT_AUTOSHIP_ON_CRITIC_PASS --body 0 --repo andrewzp/theheat`.
- **#345 eval-gating (from last handoff) still worth watching:** `cov:` nonzero-and-climbing
  in `run_history[].sources[].details.open_meteo_pipeline_metrics`.

## Standing rules
- `cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH` on
  EVERY Bash command; Python = `.venv/bin/python`.
- CI gate = the `test` job: `ruff check src/ tests/` AND `mypy src/` AND `pytest` AND
  dashboard build. Run ruff + mypy before every push.
- Stage only your own files (never `git add -A`). Land doc changes as their own PR.
- Andrew never merges. CI green → `gh pr checks <N> --watch` then `gh pr merge <N>
  --squash --delete-branch` → `git checkout main && git pull`.
- Never weaken editorial gates (thresholds, banned patterns, fact-check, critic). Voice/
  selection upgrades STRENGTHEN the bar; honesty constraints stay.
- codex-xhigh review (with `< /dev/null`, else it hangs on stdin in background) any
  plan/diff touching state/schema/storage/editorial gates/posting; loop to clean APPROVE.
- **New for Bet A:** the writer is the **metered Anthropic API** (not the Max plan) — watch
  the credit balance; a low balance silently stops drafting while runs stay green.

---

## Paste-ready kickoff prompt for a fresh session

> Pick up @theheat (`cd /Users/andrewpuschel/Documents/Claude/theheat`; `PATH=/opt/homebrew/bin:$PATH`
> + that `cd` on EVERY Bash command; Python `.venv/bin/python`; repo andrewzp/theheat).
>
> **Your mandate: read `docs/VISION.md` FIRST (the vision + upgrade brief in one doc), then
> assess the whole @theheat project and produce a detailed, prioritized upgrade plan across
> three pillars — (1) editorial excellence + sourced anecdotes, (2) global newsworthy coverage
> (Bet A), (3) reliability ("things need to stop failing" — the system fails SILENTLY:
> exhausted credits stopped the writer for hours while runs stayed green; a time-bomb test
> broke main CI; good drafts died unposted in an unwatched queue).** Then read
> `docs/handoffs/2026-07-03.md` (full session record + live state). Plan via
> `superpowers:brainstorming` → `superpowers:writing-plans` → codex-xhigh the plan → build
> incrementally behind default-OFF flags → verify live. `main` is `4dc446c` (+ #356/#357/#355
> follow-ups), version `0.9.81.0`. Anthropic credits were restored; the writer is drafting
> again.
>
> Pillar 2 (Bet A) is already mid-brainstorm — the rest of this prompt is its detail:
>
> **Your main task: Bet A — make @theheat's selection autonomously smart about
> newsworthiness + sourced human impact.** Root problem: the bot ranks by raw sensor
> magnitude and is blind to what matters — on 2026-06-29 it POSTED "1,468 MW in the Congo
> Basin" while SUPPRESSING the deadly Colorado fire (scored 62<64) that killed 3
> firefighters and led the Washington Post, and it has no path to the 1,300 European heat
> deaths. Andrew chose Bet A over a human-curated approach.
>
> **We are mid-`superpowers:brainstorming`, paused after Q3. READ
> `docs/superpowers/specs/2026-07-03-newsworthiness-bet-a-design.md` FIRST** — it has the 3
> locked decisions (hybrid source: NIFC/WHO/NWS feeds + grounded LLM search; v1 = re-rank +
> enrich what we already detect, not new-coverage-trigger; boost = rescue-capped so the
> Colorado fire is rescued from below-threshold) and the open questions. **Resume the
> brainstorm from Q4** (autonomy for impact tweets — recommend manual_only) → Q5 (MVP feeds
> — recommend NIFC + grounded search) → propose 2–3 approaches → present design → write/
> commit the completed spec → user reviews → invoke `superpowers:writing-plans` → codex the
> plan → subagent build → codex the diff → ship behind a default-OFF repo variable → verify
> live → flip on. **Before designing, READ the prior art:**
> `docs/superpowers/specs/2026-06-25-coverage-monitor-design.md` (sibling coverage-gap
> design — reconcile, don't reinvent), IDEAS.md "⭐ Requested" items #9/#10, and the GDACS
> lane. **THE IRON CONSTRAINT: every newsworthiness/impact figure comes from real cited
> retrieval (source + URL + as-of), NEVER the model's imagination — a hallucinated death
> toll is the one unforgivable error.**
>
> **Two code follow-ups before/around Bet A:**
> 1. **`fix/reganom-stakes-attribution`** (pushed, live-verified 3/3 shipping, NOT merged):
>    fixes a real defect on `main` — the merged reganom exemplar personifies France ("France
>    was effectively living…") which the honesty fact-check kills as a bare-region
>    generalization. The branch re-anchors the stakes clause to the sampled cities. **Run
>    codex-xhigh on it, then PR + merge** (independent of which France draft Andrew posts).
> 2. **The 10 pre-#352 drafts stuck in the manual queue** won't retroactively auto-ship —
>    tell Andrew to Approve+Post the strong ones (Prudhoe Bay all-time high, marine-heatwave
>    record) and Reject the old France REGIONAL_ANOMALY bulletin (nighttime-relief line).
>
> **Standing rules:** CI `test` = ruff + mypy + pytest + dashboard build; run ruff + mypy
> before pushing. Stage only your files; docs as their own PR. Andrew never merges — `gh pr
> checks <N> --watch` then `gh pr merge <N> --squash --delete-branch` when green. codex at
> `model_reasoning_effort="xhigh"` with `< /dev/null`, looped to clean APPROVE, on anything
> touching editorial gates / posting / state. Never weaken editorial gates. The writer is
> the metered Anthropic API (not the Max plan) — a low credit balance silently stops
> drafting while runs stay green. Work autonomously; stop only for a real fork or an outward
> action needing authorization.
