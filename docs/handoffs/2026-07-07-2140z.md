# Handoff — 2026-07-07 21:40Z (#401 + #403 shipped, A2 boost + per-country cap LIVE, row-13 spiked)

**`main` @ `7c7266c`**, version **0.9.94.0** (CHANGELOG `[Unreleased]`).
Supersedes [docs/handoffs/2026-07-07-evening.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/handoffs/2026-07-07-evening.md).

> **★ North star:** [docs/VISION.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/VISION.md).
> **★ Program:** [docs/superpowers/plans/front-page-parity/INDEX.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/front-page-parity/INDEX.md)

## TL;DR

Both filed editorial follow-ups shipped, **both prod flag flips went LIVE (Andrew's go)**,
and the last ungated row (13) was spiked to a **GO**. Every EXECUTABLE front-page-parity
row is merged; the rest are gated on time/data/Andrew.

## Merged this session (codex-xhigh looped clean where code)

| PR | What | main |
|---|---|---|
| [#408](https://github.com/andrewzp/theheat/pull/408) | **#401** precip monitoring-threshold cross-gate — align writer + fact-check TO the critic (`alert_threshold_mm` is internal config, never a citable scale). codex-xhigh APPROVE, 0 findings. | `9fc33e3` |
| [#409](https://github.com/andrewzp/theheat/pull/409) | **#403** coral DHW `_stress_level_for_dhw` — full NOAA scale (12→L3, 16→L4, 20→L5), fixes under-labeling. codex-xhigh APPROVE, 0 findings. | `da92aa4` |
| [#410](https://github.com/andrewzp/theheat/pull/410) | **row 13** heat-exposure design spike — Findings + INDEX status (docs only). | `7c7266c` |

**#401 validated live:** the Barrow "300 mm monitoring threshold" draft (18:07Z) was
critic-`rejected` — nothing false shipped; the fix aligns the other two gates to that.

## Flag ladder — A0/A1/A2 ALL LIVE + per-country cap active

| Flag | State | Rollback |
|---|---|---|
| `THEHEAT_NEWSWORTHINESS_ENABLED` (A0) | **1 — LIVE** 2026-07-07 12:59Z, verified | `--body 0` |
| `THEHEAT_NEWS_ENRICH_ENABLED` (A1) | **1 — LIVE** 16:23Z; P0 guard holds; **no live enriched impact draft has fired yet** (stochastic — `news_events` all NIFC fire data) | `--body 0` |
| `THEHEAT_NEWS_BOOST_ENABLED` (A2) | **1 — LIVE 2026-07-07 20:42:16Z (Andrew's go, this session)** | `--body 0` |
| `THEHEAT_PER_COUNTRY_CAP` (row 14 PR-B) | **2 — LIVE 20:42:16Z (Andrew's go)** | `--body 0` |

## ⚠️ OPEN WATCH — boost + cap, first exercised at the 00:00Z alert cycle

The boost/cap logic runs during the **alert-polling cycle** (cron `0 0,4,8,16,20` UTC) —
the flip landed 20:42Z, so the **00:00Z cycle is the FIRST to exercise it** (the hourly
`:30` runs only process the approval queue). Watch the next 2–3 alert cycles (00/04/08Z),
read-only, per [track-0-runbook.md](/Users/andrewpuschel/Documents/Claude/theheat/docs/superpowers/plans/front-page-parity/track-0-runbook.md) Step 3:

- **Regression safety first:** the cycle runs clean, no runaway; impact-citing drafts
  still `manual_only` (P0 — boost does not change decision 4). Pull state via gist
  `06c02c97ffc0d11458687f1ed998d9e5` → `state.json`.
- **Boost (rare — near-miss × matched verified news, may be days apart):** any rescued/
  killed fire in `suppressions` whose `score.reasons` carries
  `news_boost=+8 per <source> (<url>)` — provenance present, URL real; **no boost >8
  below threshold**, never without a structured/verified news match.
- **Cap:** per-country fire spill logs (fail-open allowlist; ships at 2). Confirm no
  over-suppression.
- If anything is wrong: roll the offending flag `--body 0` and investigate.

## Row 13 spike → GO (US-first, county-scoped)

Read-only spike complete (Findings in the row-13 doc). **Data path verified live:** NWS
alert `geocode.SAME` (county FIPS) → cached **1.69 MB Census CO-EST2024 CSV** (public
domain) → `POPESTIMATE2024`; near-zero marginal cost (local sum over already-fetched
alerts). Honest wording is county-scoped ("the N counties under warning are home to X
million"). World half thin (MeteoAlarm has warnings, no population) → US-first. One build
dependency: a state-kept threshold baseline. **Not data-gated — buildable when
prioritized.**

## Remaining INDEX rows (all gated)

- **Row 10** (boost beyond fire) — **GATED: A2 must be live+verified ≥1 WEEK** (do not
  build before ~2026-07-14).
- **Row 8** (FPP weekly rollup) — needs a few DAYS of `news_events` (A1 live 16:23Z today).
- **Row 9** (engagement capture) — needs Andrew's **X API tier** confirmation.
- **Row 12** (A3 new-coverage trigger) — gated 2–4 weeks of gap-flag data.
- **Row 13** — spiked GO; buildable when prioritized (see above).

## Open issues / watch (unchanged unless noted)

- [#390](https://github.com/andrewzp/theheat/issues/390) state gist ~1.44 MB (+~11 KB/
  cycle, advisory). Air-quality coverage was 100% over watched cycles.
- Held: #346 (dup-city false-record — note the **Hyderabad monthly_high** posted this
  cycle; #346 is the fix, Andrew-held), #324. #308/#309 unchanged.
- Still Andrew's: Barrow posted-tweet delete; `THEHEAT_AUTOSHIP_MAX_AGE_H` 36→96.
- Corpus re-merge `git merge origin/daily-plan-current` after a few cycles.

## Standing rules (unchanged — INDEX §Standing rules binds every task)

`cd /Users/andrewpuschel/Documents/Claude/theheat && PATH=/opt/homebrew/bin:$PATH` on
EVERY command (incl. codex + git). Before every push: ruff + mypy + 90-day canary pytest
green. codex-xhigh looped to clean APPROVE on editorial/state/posting/storage diffs, LAST
round after LAST edit. Merge = checks green → squash → `git log origin/main --oneline -1`
shows your PR. Honesty gates only tighten. Prod flag flips are Andrew's explicit words.
