# R-05 + R-07 groundwork (gathered 2026-06-14, for a fresh-session resume)

Both steps were deferred this session because each needs reference coordinates/design
that don't fit cleanly in a long context. The hard discovery work is done and recorded
here so a fresh session (one step per tranche) can finish them quickly.

## R-05 — Open-Meteo Flood (modeled) witness for `river_gauges` [CODEX]

**Endpoints (live-verified 2026-06-14, HTTP 200):**
- Discharge: `https://flood-api.open-meteo.com/v1/flood?latitude={lat}&longitude={lon}&daily=river_discharge&past_days=1&forecast_days=7` (units m³/s).
- Percentile fields exist: `daily=river_discharge,river_discharge_mean,river_discharge_median,river_discharge_max,river_discharge_p25,river_discharge_p75` all return.

**Authoritative USGS gauge coordinates (fetched from the USGS site service — `MAJOR_STATIONS` has site_ids but NO coords; never invent/mis-snap):**
```
"01389500": (40.88472222, -74.22611111),  # Passaic River at Little Falls NJ
"02084000": (35.61666667, -77.37277778),  # Tar River at Greenville, NC
"02489500": (30.7932614, -89.8209117),    # Pearl River near Bogalusa, LA
"03085000": (40.39113189, -79.8580943),   # Monongahela River at Braddock, PA
"05587450": (38.9679722, -90.429),        # Mississippi River at Grafton, IL
"06807000": (40.68180556, -95.8470833),   # Missouri River at Nebraska City, NE
"07010000": (38.629, -90.1797778),        # Mississippi River at St. Louis, MO
"07374000": (30.44566667, -91.1915556),   # Mississippi River at Baton Rouge, LA
"08066500": (30.4252067, -94.8507622),    # Trinity Rv at Romayor, TX
"09380000": (36.86433333, -111.58787222), # Colorado River at Lees Ferry, AZ
"12113000": (47.3123228, -122.2040082),   # Green River near Auburn, WA
"12340000": (46.8994111, -113.7563194),   # Blackfoot River near Bonner MT (NB: MAJOR_STATIONS calls 12340000 "Clark Fork/St. Regis" — the real gauge is Blackfoot/Bonner; use the authoritative coord)
```
USGS site service that produced these (worked from a dev IP; 403s from GH-Actions): `https://waterservices.usgs.gov/nwis/site/?sites=<comma-ids>&format=rdb&siteOutput=expanded` → columns `dec_lat_va`, `dec_long_va`.

**Two open design problems (why it was deferred, not just budget):**
1. **Impedance mismatch:** `RiverReading`/`FloodEvent` are ft-based (gauge_height_ft vs flood_stage_ft); Open-Meteo gives discharge (m³/s). A model leg CANNOT produce ft facts. Needs either optional discharge fields on both dataclasses + a model branch in `detect_floods` (which currently only emits when `above_flood and flood_stage_ft is not None`, so model readings are dropped) + a model branch in `build_river_flood_bundle` that omits ft facts — OR a separate model-flood event type. §L4 rule: the bundle must NOT contain ft facts and discharge must never masquerade as a gauge reading.
2. **Percentile semantics:** the plan says "discharge ≥ the API's 75th-percentile field". In Open-Meteo Flood, `river_discharge_p75` appears to be the ensemble 75th percentile for that day, so deterministic `river_discharge` ≤ p75 by construction — using it as a high-discharge flood threshold needs clarification (climatological vs ensemble percentile). Confirm what p75 represents before wiring the flood condition; the spec's "note the calibration assumption" acknowledges this.

Manual queue gates every draft, so an imperfect model leg can't post — but get the ft-leak and the threshold right.

## R-07 — NOAA CRW ERDDAP grid backup for `coral_dhw`

**ERDDAP endpoint (live-verified 2026-06-14, HTTP 200) — note the host differs from the plan's `coastwatch.pfeg.noaa.gov` (that 404s); the correct host is `coastwatch.noaa.gov`:**
- Dataset: `https://coastwatch.noaa.gov/erddap/griddap/noaacrwdhwDaily`
- Variable: `degree_heating_week` (matches plan), `_FillValue = -327.68`, dims `time, latitude, longitude`, 0.05° grid (lat/lon −89.975..89.975).
- Point query (CSV): `…/noaacrwdhwDaily.csv?degree_heating_week%5B(last)%5D%5B({lat})%5D%5B({lon})%5D` → returns the nearest cell's latest DHW. Example (GBR −18,147): `2026-06-08T12:00:00Z,-17.975,147.025,0.0`. Latest data ~6 days old (coral is slow).

**Obstacle (the correctness core):** the witness fires when the CRW *station-text* host is down, so it can't read the station list/coords from the primary. It needs a **hardcoded station→grid-coordinate map** (region_id → lat/lon) for the CRW virtual stations to sample the grid. The CRW index (`https://coralreefwatch.noaa.gov/product/vs/data.php`) loads locally (HTTP 200, ~1.4 MB HTML) and is the authoritative coord source, but the per-station lat/lon parsing wasn't trivial in the time available — `_parse_station_index` returns only region_id/name/stress/data_file; coords are parsed per station file by `_fetch_station_latest`. A future session should either (a) parse the index once locally to build a curated region_id→(lat,lon) map for the major reef regions and commit it as a tested constant, or (b) reuse coords from the S-15 last-good cache as the sampling locations (the fresh grid DHW is the fact; cached coords are just where to sample — but verify this doesn't trip the evidence-contract cached-fact rejection). Test the mapping hard: a mis-mapped reef reports the wrong reef's DHW.

The same-provider grid leg carries `evidence_grade="observed_alt_host"` (different product/path than the station text).
